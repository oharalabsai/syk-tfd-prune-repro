# syk-tfd-prune — campaign report

**Full paper: [paper/main.pdf](paper/main.pdf)** (LaTeX source: paper/main.tex)

Cycles: 16 · Experiments: 3 · Promotions: 3 · Final incumbent score: 25.8

> Draft artifact. Before this leaves the building it must pass docs/paper-audit-protocol.md; human-only byline + AI-contribution statement.

## Abstract

Preparing the thermofield double (TFD) state is the entry cost of sparse Sachdev-Ye-Kitaev (SYK) wormhole and scrambling experiments, a cost dominated by two-qubit gates. A recent preprint proposes preparing it with a multi-angle QAOA ansatz reduced by sequential angle pruning (SAP) and its ordered variant (OSAP), and does not include a comparison with a 35-CNOT block. That fixed hardware-efficient block is the authors' own hardware TFD circuit, and they told us it transpiles to 35 CZ on ibm_marrakesh. We test the preprint's pruning against that block on five certified sparse-SYK instances at inverse temperature beta = 3, each on eight qubits in its own Jordan-Wigner map, with the preprint's pruning algorithms, optimizer budgets and candidate-pool size and two disclosed deviations (two weight-2 mixer strings treated as prunable blocks; a pre-registered small-angle start on four instances). Here we show a clear null on four instances and a borderline case on the fifth. On Rows 42, 48, 32 and 30, pruned ma-QAOA at 35 two-qubit gates falls short of the block by 0.06 to 0.14 under every definition we checked (routed or logical axis; converged, best-of-6 or contract anchor). On Eq. 11, OSAP reaches F = 0.925 at 35 logical CNOTs, above the contract's sealed anchor (0.923) and 0.014 below the converged anchor (0.938), but that circuit routes to 43 CZ, and within 35 routed CZ the best is 0.904. The pre-registered decision rule (routed axis, converged anchor, 0.02 margin) returns NULL on all five instances; on Eq. 11 that verdict depends on those axis and anchor choices, and under the campaign's original frozen contract (logical axis, sealed anchor) Eq. 11 would not be a null. At <= 35 routed CZ the best fidelities are 0.802 to 0.904 (OSAP) and 0.766 to 0.821 (SAP), against a converged anchor of 0.936 to 0.949. Reaching the converged anchor costs OSAP 93 to 103 logical CNOTs on four instances and 41 on Eq. 11, and OSAP dominates SAP, consistent with the preprint. A by-product favours the block: its converged own-map anchor exceeds the 0.887 to 0.915 of our earlier study, with encoding and fit effort (6 x 800 to 50 x 3000) changed together.

## Belief state at close

# PRIORS — syk-tfd-prune

Belief state. Every claim: `[STATUS] claim (evidence)`. Wrong beliefs get status-flipped in place, never deleted.

- [ALIVE] baseline scores 99.6580 on the hidden battery (EXP-0000)
- [OPEN] ELI5: right now our program ignores the physics problem entirely. It plays the same fixed sequence of 35 entangling moves no matter which instance it is handed, so the state it makes is essentially a random state and matches the one we want about 0.3% of the time. This hypothesis says: if the program actually reads the instance's couplings, builds the exact state it is supposed to make, and then fits a physics-shaped circuit (multi-angle QAOA with the SYK cost layer and the left-right mixer) to that state, then prunes the least useful angle blocks away until the circuit fits inside the 35 two-qubit-gate budget, the match jumps from a fraction of a percent to most of the way there. Formally: an instance-aware ma-QAOA ansatz (per-term cost angles e^{-i*gamma_{t,l}*P_t} on the Jordan-Wigner images of the sparse-SYK quartets, per-pair mixer angles on the left-right Majorana coupling i*psi^L_j*psi^R_j), variationally fitted to the exact beta=3 TFD reconstructed by the candidate itself from the payload, then reduced by sequential angle pruning (SAP, arXiv:2609.02793 Algorithm 1) to a logical two-qubit cost C2 <= 35 including the |I>-preparation prefix, scores dramatically better than the untuned fixed block. This is the first cycle: it establishes that the SAP pipeline runs end to end inside prepare() and produces a valid, in-budget, instance-dependent circuit, and it gives the campaign its first real point on the fidelity-vs-cost curve at the matched cost C2 = 35. — challenged: Four blocking objections. (1) COMPUTE BUDGET IS OFF BY ~5 ORDERS OF MAGNITUDE, so the run will not execute the hypothesis it pre-registers. Section 5 specifies SAP Algorithm 1 literally: one prune per round, and every round trial-removes EACH still-active block with a full warm-start L-BFGS-B re-optimization (maxiter 300) over the surviving angles, with jac=None finite differences explicitly permitted. With the brief's own cap of B<=220 blocks, one FD gradient costs B+1 objective evals, one re-optimization costs up to 300*(B+1) ~ 6.6e4 evals, one round costs B*6.6e4 ~ 1.5e7 evals, and the loop needs ~200 rounds to fall from ~220 blocks (>=1000 logical CNOTs at >=2 CNOTs per block) to <=35 CNOTs: ~3e9 objective evaluations per instance. Each evaluation is a ~1000-2000 gate dense 256-dim simulation, i.e. milliseconds at best. That is days-to-weeks per prepare() call against the brief's own 600 s net in section 6. Consequence: the time budget trips on every instance and the fallback path fires, which section 6 defines as 'prune the lowest-|angle| blocks without reoptimization'. The cycle therefore measures MAGNITUDE PRUNING, not SAP, while the claim, falsifier and reopen_condition are all written about SAP. Analytic gradients do not rescue this (a 10x-30x constant does not close 1e5). Required before proceeding: measure the per-objective-eval cost and the O(B^2) round count, then restate the pruning schedule that actually fits (e.g. prune k blocks per round, or start from a p and block set small enough that B is tens, not hundreds), and make the falsifier name the schedule that was actually run.

(2) THE KILL IS NOT IDENTIFYING. Section 6's graceful degradation returns workspace/baseline.py's untuned block on ANY exception, on self-F < 0.05, or on selector mismatch > 1e-3. That circuit is exactly the incumbent, so it scores 99.658 (verified: experiments/EXP-0000-baseline/verdict.json, valid=true, cost exactly 35, no penalty). 99.658 > 89.66, i.e. the kill threshold. A missing payload key, a wrong |I> convention, a target-selector failure or the timeout in (1) is therefore indistinguishable in the verdict from 'SAP-pruned ma-QAOA at C2<=35 is useless', yet the kill_threshold declares that whole direction DEAD and forbids re-proposal absent four named new facts. The brief itself flags in section 0 that the payload key set 'is not guaranteed', so the fallback path is a live probability, not a remote contingency. A kill this consequential needs a mechanically distinguishable failure signal (e.g. an unmistakable non-baseline degenerate circuit for the fallback) or the kill must be conditioned on a run that provably did not degrade.

(3) THE BUDGET ARITHMETIC IN THE BRIEF IS WRONG, AND NO REACHABILITY EVIDENCE IS OFFERED. Section 4 asserts 'typical support is small, so many mixer blocks cost 2 CNOTs'. Under the contract's stated JW convention (g_{2k} = Z_0..Z_{k-1} X_k per side) the mixer generator i*psi^L_j*psi^R_j is a product of two Z-tailed strings, one per register: support = (floor(j/2)+1) on each side, so cost = 2*(support-1) runs 2, 2, 6, 6, 10, 10, 14, 14 CNOTs for j=0..7, mean 8, not 2. Cost blocks are 4-body on 4 left qubits, so 6 CNOTs each. After the 4-CNOT |I> prefix only 31 CNOTs remain, i.e. roughly FOUR surviving blocks, and six of the eight mixers individually cost 6-14. The prediction (mean F >= 0.60) is asserted for a circuit of ~4 blocks with no reachability argument at that surviving-block count, which is exactly what the answer-first R5 gate forbids: prove the bar is reachable before chasing it. Cheap and mandatory first: exhaustively/greedily evaluate the best <=31-CNOT ma-QAOA block subset on the primary instance and report its F.

(4) THE CONTRACT'S OWN PRE-REGISTERED FIRST GATE IS SKIPPED. CONTRACT.md kill criteria open with the REACHABILITY KILL: un-pruned ma-QAOA reaching F >= 0.9999 on the primary instance by p=8 under a named initial-fit budget (5 inits from default_rng(100+p), maxiter 3000, ftol 0, gtol 1e-10), shipping the F(p) table for p=3..8. The brief caps P_LAYERS at 4 (section 5), reuses that seed rule for a different purpose, and never produces the p=3..8 table. Pruning a ceiling that has not been shown to exist inverts the contract's order: if the un-pruned ansatz cannot reach the target, the pruned curve is uninterpretable. Run the reachability table first; it is also the cheapest thing in the campaign and it fixes the p and block count that objection (1) needs.

Non-blocking but fix while revising: section 1's selector compares the reduced spectrum against softmax(-beta*eig(H_L)) at 1e-8, which presumes the TFD is exactly (e^{-beta H_L/2} x I)|I>; T3/T4 (the H_cost = H_L + H_R convention the Director's 2026-09-21 23:20Z note says the scorer actually uses) will generally NOT satisfy that at 1e-8, so the selector can reject the correct construction and trip the 1e-3 fallback in section 6. State which normalization of beta each admissible construction is being tested under, or test the selector on both beta and beta/2 conventions. (HYP-0001)
- [OPEN] ELI5: our program currently plays a fixed 35-move sequence that ignores the problem, and matches the wanted state about 0.3% of the time. The challenger showed that the physics-shaped moves are EXPENSIVE: one left-right mixer move costs 2 to 14 two-qubit gates and one SYK cost move costs about 6, so a 35-gate budget buys only about five moves, not hundreds. So the honest first question is not 'how do we prune hundreds of moves down' (that costs billions of simulations and would time out, which is the challenger's objection 1) but 'what is the BEST five-or-so-move physics circuit, and how good is it?'. This hypothesis answers exactly that, which is what challenger objection 3 demanded: build the exact target state from the payload, enumerate the admissible ma-QAOA block pool, and do FORWARD greedy selection under the hard 35-CNOT budget, re-optimizing all selected angles at every step, instead of SAP's backward O(B^2) prune. Formally: an instance-aware multi-angle QAOA block pool (per-term Jordan-Wigner cost blocks on the left register, their right-register mirrors, and the eight single-pair left-right Majorana mixers i*psi^L_j*psi^R_j), each block exp(-i*theta*P/2) compiled as a CNOT ladder of cost 2*(weight(P)-1), selected greedily under C2 <= 35 with full warm-start L-BFGS-B re-optimization of all live angles at each step and exact adjoint gradients, starting from the payload's reference state, scores dramatically better than the instance-blind incumbent and produces the campaign's first real, cost-honest point on the fidelity-vs-cost curve plus the measured in-budget block count B*. — challenged: Two blocking objections; both are cheap to fix before compute, and one is already conceded inside the brief itself.

(1) THE CYCLE IS PRE-REGISTERED AS A COIN FLIP ON AN UNRESOLVED CONVENTION, AND THE BUDGET ARITHMETIC DEPENDS ON WHICH SIDE LANDS. The brief's own closing NOTE FOR THE DIRECTOR states the contradiction: CONTRACT.md's dev-examples section says the scorer simulates the candidate's gate list from payload['reference_state'] = |I>, while the JOURNAL entry of 2026-09-21 23:20Z records the Director REJECTING that scorer and replacing it, so that the fixed scorer 'loads tfd_targets.npz and simulates every candidate from |0>^8', with the same entry explicitly noting 'ma-QAOA's |I> reference is maximally entangled (not a product state), so its |I> prep costs 2q gates; logical axis counts full |0>->TFD'. The journal describes the scorer that was actually installed and against which the baseline 99.6580 was locked; the contract text is the pre-fix draft's description. The hypothesis knowingly builds to the superseded convention and pre-registers a VOID clause to absorb the failure. That is a full run of five prepare() calls, plus the unbounded Section 7 diagnostic, spent to learn a fact one Director question answers. This is the challenger's 'simpler existing path to the same information' gate: ask which start state and which target construction the sealed scorer uses, then write the candidate against the answer. I have not opened the scorer myself (rule 4) and neither should the coder; the resolution belongs to the Director.

The consequence is not merely VOID-vs-science, it is arithmetic. If the scorer starts from |0>^8, Section 4's C2_BUDGET = 35 allocated entirely to ma-QAOA blocks is wrong: the maximally entangled |I> prefix consumes 2q gates first (PRIORS HYP-0001 objection 3 already priced this at 4 CNOTs, leaving 31), so the greedy would overspend the cap on every instance and the emitted cost assertion in Section 5 would certify a number the scorer does not agree with. The brief must state the budget as (prefix cost + block cost) <= 35 under the |0>^8 convention, or state |0>^8 is excluded by a Director ruling.

(2) SECTION 1'S SELECTOR IS VACUOUS AS A CONVENTION CHECK, SO IT CANNOT CATCH THE SECOND FORK. The check tests that reduced_left(psi_target) has eigenvalues softmax(-beta*eig(H_L)) to 1e-8. psi_target is the candidate's OWN construction kron(expm(-beta*H_L/2), eye(16)) |S>, which satisfies that identity by construction for the correct kron order regardless of what the scorer's sealed target is. It therefore validates bit ordering only, and cannot detect the second, independent fork the same journal entry records: the Director replaced the '(e^{-bH_L/2} x I)|I> TFD shortcut' and the '4-left/4-right naive |I>' with GIST Eq. 5 shared-qubit encoding and the fermionic |I>, built by seal_targets.py. If the sealed target is built that way, the candidate optimizes to a different state, F_self is high, the scored F is low, and the cycle lands in VOID for the second reason. The same PRIORS non-blocking note flagged exactly this selector for H_cost = H_L + H_R. Either name, from the Director, the construction the sealed targets actually use and build psi_target that way, or add a check that is falsifiable against the scorer (there is none available to the candidate, which is itself the argument for resolving it at the Director before the run).

Non-blocking, fix while revising: Section 7 is mandatory ('you MUST run and report') but carries no wall clock, unlike Section 4's 240 s. With the full pool at p=8 the angle count runs into the hundreds and the spec is 5 inits x 3000 L-BFGS-B iterations x 6 values of p; that is an open-ended dev loop with no stated stopping rule. Give it an explicit wall clock and a partial-table reporting rule, so a slow p=7 does not silently consume the cycle's window.

What is NOT objected to, so the revision does not over-correct: the forward-greedy schedule with exact adjoint gradients is a genuine, arithmetically plausible answer to HYP-0001 objection 1 (O(m) Pauli applications per objective+gradient, tens of greedy steps, comfortably inside 240 s), the MARKER at C2 == 0 with no import of workspace/baseline.py is exactly the mechanically distinguishable failure signal objection 2 demanded, and Section 7 is the right advisory form of the contract's REACHABILITY gate that objection 4 flagged. Resolve the start-state and target-construction conventions with the Director, restate the budget line under the ruling, and this is executable. (HYP-0002)
- [OPEN] ELI5: our program still plays a fixed 35-move sequence that ignores the instance and matches the wanted state about 0.34% of the time. Two cycles have now died not on physics but on a bookkeeping fork: nobody was certain which starting state the grader simulates from, or exactly how it builds the state we are aiming at, so the last cycle stopped and asked the Director. This cycle stops waiting. It settles the start-state question from the Director's OWN written record (the 2026-09-21 23:20Z JOURNAL entry, which states the installed scorer 'loads tfd_targets.npz and simulates every candidate from |0>^8' and that 'logical axis counts full |0>->TFD' - that is a Director statement, not a guess), and it settles the target-construction question mechanically, from the payload alone, by enumerating the small set of admissible target constructions, ELIMINATING the ones the payload itself falsifies, and then fitting a circuit that is as good as possible against the WORST surviving one. Formally: an instance-aware candidate that (a) builds a target SET T = {T_1..T_K} of admissible beta=3 TFD constructions from payload['h_left']/payload['terms'] and payload['reference_state'], prunes it with the maximally-entangled transpose identity (A (x) I)|I> = (I (x) A^T)|I>, which is falsifiable against the payload rather than satisfied by construction (this is what PRIORS HYP-0002 objection 2 demanded and the softmax selector failed to be), (b) prepares a maximally entangled scaffold from |0>^8 with an explicitly measured prefix cost, (c) selects ma-QAOA blocks (Jordan-Wigner images of the sparse-SYK quartets on the left register, their right-register mirrors, the eight left-right Majorana mixers i*psi^L_j*psi^R_j) by an ADAPT-VQE-style analytic-gradient screen plus forward greedy under the hard budget PREFIX_COST + BLOCK_COST <= 35, re-optimizing all live angles with exact adjoint gradients, and (d) emits, per instance, the circuit with the best WORST-CASE fidelity over the surviving target set. The scientific content is the first cost-honest point (and the first fidelity-vs-cost curve) of ma-QAOA at matched logical cost, obtained without a blocking Director round-trip; if it falls far short of F35 that is the pre-registered MATCHED-COST NULL's first certified digits, which CONTRACT.md already declares a shippable QUESTION-STATUS outcome. — challenged: Two blocking objections. Both are about mechanisms the brief itself advertises as its answers to prior challenges, and both are cheap to fix before compute.

(1) THE ENUMERATION OMITS THE ONLY FORK THE DIRECTOR ACTUALLY NAMED, AND ITS ELIMINATION TEST GOES VACUOUS EXACTLY WHEN THAT FORK IS LIVE. Section 0 correctly rules that CONTRACT.md's dev-examples paragraph describes the REJECTED draft scorer, then Sections 1 and 2 build the entire target set from that same rejected paragraph's payload keys and its 4-left/4-right kron split. Every one of T1..T5 is kron(f(H), g(H)) @ R on a 4+4 tensor factorization; they differ only in beta normalization and which side carries the exponential. The 2026-09-21 23:20Z JOURNAL entry, which the brief cites as its authority for Section 0, names TWO defects in the draft, and the brief inherits the second one wholesale: the Director replaced 'a 4-left/4-right qubit split and naive |I> = sum|x>|x>' with 'GIST's Eq. 5 shared-qubit encoding and the fermionic |I>', built by seal_targets.py. If the sealed targets use that encoding, the sealed construction is not in {T1..T5} for any beta, so the worst-case hedge in Section 5 hedges over five members of the wrong family. That is a missing completeness argument in the sense answer-first R3 forbids: the enumerated family is 'beta conventions and transpose sides', while the fork on the record is the ENCODING. Enumerate the encoding axis too (naive 4+4 split and the Eq. 5 shared-qubit / fermionic-|I> construction, the latter built from payload['reference_state'] rather than assumed), or state that the encoding axis is excluded by a named Director ruling.

Worse, the elimination test cannot catch this, for precisely the reason the softmax selector was killed in PRIORS HYP-0002 objection 2. Section 1's fallback sets R = psi_naive = (1/4) sum_x |x>_L|x>_R when payload['reference_state'] is absent, and that key's existence is guaranteed only by the rejected draft's payload description. psi_naive is maximally entangled in exactly the ordering the test assumes, so o12 and o13 both come out 1 BY CONSTRUCTION, the run reports ordering_confirmed = true, K = 2, and the DIAG line records a convention 'measurement' that measured nothing. The test is falsifiable only when R is genuinely read from the payload, which is the branch the transpose identity was introduced to certify. Required fix: gate the test on provenance. If R came from the payload, run it and report the result. If R came from the fallback, report ordering_confirmed = 'untested (R synthesized)', treat the test as vacuous, and hedge over the full K = 5 set (plus the encoding members from the paragraph above) rather than collapsing to K = 2. As written, the single most likely payload shape produces a spuriously confirmed convention and a confidently wrong K.

(2) THE ADVISORY REACHABILITY TABLE IS RUN INSIDE A SCORED prepare() CALL WITH NO STATED SCORER TIME LIMIT, AND IT CAN INVALIDATE INSTANCE 1 FOR A NON-PHYSICS REASON. Section 6 budgets 600.0 s for the table and runs it 'ONCE, on the first instance prepare() sees'; Section 4 budgets a further 240.0 s of greedy for that same instance. Instance 1's prepare() is therefore pre-registered to run up to 840 s, while instances 2-5 run up to 240 s. The brief never states the scorer's actual per-call or total wall-clock limit, and it cannot be read (rule 4). HYP-0001's brief asserted a 600 s net figure, which if it reflects the real limit means this cycle's FIRST scored call is pre-registered to exceed it by 40%. The consequence is not a slow run: a timed-out or killed prepare() returns invalid on INSTANCE 1, which fires falsifier (1) ('the scorer returns invalid on any instance'), and the MARKER path in Section 7 does not catch it because a wall-clock kill is not an exception the try/except sees. A run that dies on an advisory table that by the brief's own words 'never affects any emitted circuit' would be indistinguishable in the verdict from a cost-model disagreement, which is the non-identifying-failure defect PRIORS HYP-0001 objection 2 already forced this campaign to fix once. Required fix: either move the reachability table out of the scored path entirely (run it in a separate dev script and report its table in the hypothesis record, not inside prepare()), or bound the TOTAL time of any single prepare() call by a stated constant that the brief justifies, with the table's budget being whatever remains after the greedy's 240 s rather than additive to it.

Non-blocking, fix while revising: (a) Section 4's ADAPT screen ranks by (screen gain)/(member cost) using the first-order derivative at theta = 0, which vanishes identically for any generator whose gain is second-order at the current point; with a maximally entangled scaffold and a cold start this is a real possibility for whole symmetry classes of the pool, and a screen that returns all-zero gains will silently select by tie-break (lower cost, then lower pool index) rather than by physics. State the fallback explicitly: if the top screen gain is below 1e-12, fall back to trialling the 3 cheapest affordable members and record screen_degenerate = true in DIAG. (b) Section 5's worst-case selection over K targets is stated without a floor; at K = 5 spanning genuinely different constructions, min_k |<T_k|C|0>|^2 can be driven to near zero by one hostile member while the prediction still claims >= 0.20 mean scored fidelity. Say what the candidate emits when the best worst-case w_j is below, say, 0.02: emitting the argmax-w circuit is still correct, but the DIAG must flag hedge_collapsed = true so the Director can read the resulting low score as the cost of the unresolved fork rather than as the physics null.

What is NOT objected to, so the revision does not over-correct: settling the start state from the Director's written journal entry instead of blocking on Q-GD-0009 is legitimate and is the right reading of that record; the explicit PREFIX_COST + BLOCK_COST <= 35 budget line answers HYP-0002 objection 1; the numerical Pauli-label extraction with the single-string and Hermiticity assertions is the right way to answer HYP-0001 objection 3 (measure the cost table, do not trust the brief's expected 2,2,6,6,10,10,14,14); the shared-|phi>/|chi> screen with supplied analytic jac is arithmetically sound and is thousands of times cheaper than the SAP schedule priced dead in HYP-0001 objection 1; and the MARKER at C2 == 0 with no import of baseline.py remains the correct mechanically distinguishable failure signal. Fix the encoding axis and the provenance gate on the elimination test, move or bound the reachability table, and this is executable. (HYP-0004)
- [OPEN] ELI5: after four cycles this campaign has run zero experiments. Every cycle died on the same bookkeeping fork: nobody has established, by measurement, which exact state the grader is grading us against. Meanwhile there is a number we already know is reachable: the campaign's own sealed anchor says that the SAME 35-gate circuit shape the current program already plays reaches about 89% match once its angles are FITTED to the instance, versus 0.34% with the untuned angles it plays today. So the entire 99.3-point gap between where we are and the anchor is angle fitting, and angle fitting needs exactly one thing: the target state. This cycle therefore does the cheapest possible decisive thing. It keeps the circuit SHAPE byte-identical to the incumbent (6 layers of rz/rx on 8 qubits interleaved with 5 linear CNOT chains, exactly 35 logical CNOTs, no structural change at all), builds the target state from first principles out of the payload, and fits the 96 angles to it. Formally: the candidate constructs the 16 Majorana operators on the 8-qubit register under BOTH admissible Majorana-to-qubit encodings (E1 block Jordan-Wigner, left Majoranas on qubits 0-3 and right on qubits 4-7; E2 shared-qubit/interleaved Jordan-Wigner, psi^L_j = g_{2j} and psi^R_j = g_{2j+1} on one 8-qubit chain), obtains |I> in each encoding NOT by assuming a form but by solving the fermionic annihilation condition (psi^L_j + i psi^R_j)|I> = 0 for all j, forms the admissible targets T ∝ exp(-s*beta*H_L)|I> over the enumerated encoding x |I>-variant x beta-normalization grid, eliminates members using only provenance-gated falsifiable payload evidence, commits to the highest-priority survivor, and fits the incumbent's own 96-angle block to it with adjoint-gradient L-BFGS-B under the contract's sealed refit protocol (6 inits, 3000 maxiter, ftol 0, gtol 1e-10). If the committed construction is the sealed one, the score collapses from 99.658 to roughly 11, reproducing the sealed F35 anchor to the digit and confirming the convention in the same run. If it is not, the score stays at ~99.6 and the campaign has, for the first time, a MEASURED refutation of a named construction instead of a fifth round of argument. Both outcomes are one bit of ground truth for the price of one cheap run, and the ma-QAOA + SAP structural comparison the DIRECTION asks for becomes executable in cycle 6 against a confirmed target and a reproduced anchor. — challenged: Two blocking objections, both cheap to fix before compute. I verified workspace/baseline.py: Section 4's topology (LAYERS=6, N_CHAINS=5, 8 qubits, rz then rx per qubit per layer, cx(q,q+1) for q=0..6 after layers 0..4, 131 gates, 35 logical CNOTs, theta shape (6,8,2)) is gate-for-gate the incumbent block, and CONTRACT.md's integrity clause (i-b) independently confirms the sealed anchor is a 6x8x2 array. The anchor-reproduction premise is sound and is NOT what I am objecting to.

(1) THE MOST LIKELY EXECUTION PATH SILENTLY COMMITS TO C2 WHILE THE PREDICTION, KILL THRESHOLD AND DECISION TABLE ARE ALL WRITTEN ABOUT C1. Section 3's T-A fires whenever payload['h_left'] is present with shape (16,16), which is exactly the shape CONTRACT.md's payload paragraph documents, so this is the expected branch, not a corner case. On a match T-A drops C1, C4, C5, C6, i.e. every E2 member, and Section 3's 'commit to survivors[0]' then commits to C2. Nothing else in the artifact tracks that switch: the prediction is stated for 'the committed construction C1', the kill threshold reads 'it mechanically refutes the committed conjunction (start state = |0>^8) AND (sealed target = C1 = encoding E2, fermionic |I>, s = 0.5)', and Section 8's rows are keyed to C1 with cycle 6 walking to C2. A run that actually tested C2 would therefore be recorded as killing C1 and would send cycle 6 to re-test the member it just tested. The DIAG dict does not rescue this: the brief itself says the harness does not read it, so the verdict is undecodable against Section 8 from the ledger alone.

T-A's inference is also unsound on its own terms. A 16x16 h_left in the payload is a convenience representation of the quartet couplings on four qubits; it is evidence about what the payload exposes, not about which Majorana-to-qubit map seal_targets.py used to build the sealed target. The scorer can hand out a 4-qubit H_L and still build the target in the shared-qubit encoding from the same 'terms'. So the single test most likely to fire eliminates the hypothesis's named subject on an inference that does not follow. Required fix, pick one and state it: (a) demote T-A (and T-B, which has the same character) to observation-only, record the comparison result in the report and drop nothing, so this cycle unconditionally commits to C1 and the one-bit readout is about the member the hypothesis names; or (b) keep the elimination and rewrite the prediction, falsifier, kill threshold, reopen condition and Section 8 to be keyed to `committed` rather than to C1, and state which member each decision-table row applies to under each possible provenance value. T-C is fine as written and is genuinely falsifiable, since R is read rather than synthesized; the objection is to T-A/T-B only.

(2) THE WALL CLOCK TRUNCATES THE SEALED REFIT PROTOCOL, AND THE SURROGATE THAT THE KILL THRESHOLD LEANS ON IS RUN WITHOUT IT, SO THE SURROGATE DOES NOT CERTIFY THE SCORED PATH'S FIT. Section 5.2 copies the sealed BASELINE FAIRNESS HOLD budget (6 inits x 3000 L-BFGS-B iterations, ftol 0, gtol 1e-10) precisely so that a match reproduces the anchor 'under the anchor's own budget'. Section 5.5 then caps the whole prepare() call at 120 s, starts no new init after 100 s, and aborts the running optimizer at 115 s. The brief never prices one objective-plus-gradient evaluation, so it never checks that the two are compatible. On the arithmetic it invites: one forward sweep is 131 gate applications on a 256-dim state, the adjoint gradient is about three sweeps, and L-BFGS-B with maxiter 3000 and ftol 0 / gtol 1e-10 on 96 parameters will typically run thousands of function-and-gradient evaluations per init before either stopping criterion bites. Six inits at even a few milliseconds per evaluation does not fit in 100 s. The expected outcome is that three or four inits run and the last one is cut mid-optimization, i.e. the cycle measures a truncated fit while the hypothesis is pre-registered on the sealed budget.

That by itself would be a labeling problem. What makes it blocking is that the kill threshold's identifiability argument depends on the gap. It reads: 'Because section 7's offline surrogate study independently certifies that the same fitted block reaches F >= 0.80 against its OWN construction, a no-improvement verdict at cost 35 cannot be a fitting failure or a budget failure.' Section 7 specifies no wall clock at all for the surrogate, so the surrogate certifies fitting power under the untruncated 6x3000 budget while the scored path runs a truncated one. A no-improvement verdict is then exactly as consistent with 'the scored fit was cut at 115 s' as with 'C1 is not the sealed construction', which is the non-identifying-failure defect PRIORS has already forced this campaign to repair twice (HYP-0001 objection 2, HYP-0004 objection 2). Required fix, both parts: (a) measure one objective-plus-gradient evaluation and the typical evaluations-to-convergence in the Section 7 surrogate, report those numbers, and set the number of inits and the maxiter that actually fit inside the stated wall clock, pre-registering THAT as this cycle's fit protocol with the deviation from the sealed 6x3000 budget stated explicitly in the hypothesis record; and (b) run the surrogate self-fit under the identical Section 5.5 guard and the identical init count, so the F >= 0.80 precondition certifies the fit the scored path actually performs. Also record per-init nit, nfev, termination status and final gradient infinity-norm and whether the guard fired, mirroring the contract's own fairness-hold bookkeeping, and report `wall_clock_truncated` so the Director can distinguish a truncated fit from a convention refutation.

Non-blocking, fix while revising: (a) Section 4 forbids importing workspace/baseline.py, which is correct for the fallback-identifiability reason of Section 6, but the brief should still require the coder to READ baseline.py and assert the re-implemented gate list is element-for-element equal to block_gates(theta) for a fixed theta; the entire anchor comparison rests on byte-identical topology and that check costs nothing (note baseline.py's loop guard is `layer < N_CHAINS` with N_CHAINS = 5, which coincides with Section 4's `layer < 5` but is a different expression, so assert rather than eyeball). (b) Section 5.3's adjoint recipe is ambiguous about ordering: it says to undo the gate on both |phi> and |lam> and to accumulate, without saying that the accumulation at gate k uses |phi_k> BEFORE the undo and |lam_k> BEFORE its undo. The off-by-one this invites is caught by the import-time finite-difference check, so it costs a dev round rather than a cycle, but state the order explicitly. (c) Section 2.2 should record, not merely assert, that under E2 the solved |I> comes out as the computational product state |0>^8 (A_j = g_2j + i g_2j+1 is 2 c_j, whose common kernel is the JW vacuum) and under E1 it does not; a coder expecting a 'maximally entangled' vector may read |0>^8 as a bug and take the MARKER path. (d) Section 2.3's pairwise-overlap table is computed and reported but nothing acts on it; state the action, since reopen condition (C) already depends on it. (e) Give Section 7's surrogate an explicit wall clock and a partial-table reporting rule, the same discipline objection (2) of HYP-0004 demanded of the in-path table.

What is NOT objected to, so the revision does not over-correct: settling the start state from the Director's own 2026-09-21 23:20Z journal entry instead of blocking a fifth cycle on Q-GD-0009 is the right reading of the record and the right call; retiring the prefix-cost line follows from it; holding the topology fixed to preserve the anchor comparison is the correct scope discipline and is what makes this the cheapest decisive cycle available; the enumeration in Section 2.3 finally covers the ENCODING axis that was the blocking objection against HYP-0004, with a completeness argument that names its three axes; solving the annihilation condition instead of assuming a kron form is the right answer to HYP-0004's assumed-|I> defect; the MARKER at cost 0 with no import of baseline.py remains the correct mechanically distinguishable failure signal; and the Section 8 decision table with its explicit AMBIGUOUS MIDDLE is exactly the R6 pre-registration this campaign has been missing. Unconditionally commit to the member the hypothesis names, and make the fit budget one that the wall clock and the surrogate both honor, and this is executable. (HYP-0005)
- [OPEN] ELI5: after five cycles this campaign has still run zero experiments, and the last two died on the same two flaws, not on physics. Cycle 5's design was going to (a) throw away the very thing it said it was testing, because a guess about what one payload key implies would have silently switched the target out from under the hypothesis, and (b) promise a fitting budget the clock could not pay for, so a bad score could have meant 'wrong physics' or just 'we ran out of time' with no way to tell. This cycle fixes both by removing choices instead of adding machinery. It commits, unconditionally and in advance, to ONE named target construction, the one the Director's own written record names. It keeps the circuit shape byte-identical to the incumbent (6 layers of rz/rx on 8 qubits interleaved with 5 linear CNOT chains, exactly 35 logical CNOTs, not one gate added or moved), so a good score reproduces the campaign's own sealed 89% anchor to the digit. And it sets the number of restarts and iterations from a MEASURED cost per optimizer step, pinned as constants before the scored run, with the same budget re-run offline against a known-answer instance so that a no-improvement verdict provably cannot be a fitting failure. Formally: the candidate builds the sixteen Majorana operators on the 8-qubit register under the shared-qubit (interleaved) Jordan-Wigner encoding E2 (psi^L_j = g_{2j}, psi^R_j = g_{2j+1}), obtains |I> by SOLVING the fermionic annihilation condition (psi^L_j + i psi^R_j)|I> = 0 rather than assuming a kron form, forms T = normalize(exp(-0.5*beta*H_L(E2))|I>) with beta = 3, and fits the incumbent's 96 free angles to T from |0>^8 by adjoint-gradient L-BFGS-B under a pre-measured, pinned budget. The enumeration of the six alternative constructions is still computed and reported as observation, but nothing is eliminated and nothing is switched: the cycle buys exactly one bit about one named construction, for the price of one cheap run, and either reproduces the anchor (score ~11 instead of 99.658) or delivers the campaign's first MEASURED refutation of a named convention instead of a sixth round of argument. — challenged: Two blocking objections, both arithmetic, both cheap to fix before compute. I verified workspace/baseline.py directly: block_angles() and block_gates(theta) exist with LAYERS=6, N_CHAINS=5, BLOCK_QUBITS=8, loop guard `layer < N_CHAINS`, rz-then-rx per qubit per layer, cx(q, q+1) for q=0..6, giving 131 gates and 35 logical CNOTs. Section 4's mandatory element-for-element assertion is executable exactly as written, and the anchor-topology premise is sound. I also checked section 2.2's algebra by hand: under E2, A_j = g_{2j} + i g_{2j+1} = (Z-string) (x) (X_j + iY_j) and X + iY = 2|0><1|, so the common kernel is |0>^8 and the brief's i_e2_is_vacuum expectation is correct. Neither of those is what I am objecting to.

(1) THE KILL THRESHOLD IS SET AT A SCORE THE BRIEF'S OWN SUPPORTING PREDICTION SAYS A FALSE HYPOTHESIS WILL NOT REACH, SO THE ADVERTISED ONE-BIT READOUT DOES NOT EXIST. The kill requires mean score >= 99.5580, i.e. mean recomputed fidelity against the sealed target below 0.0042, which is BELOW the untuned random seed-0 block's measured 0.00342 (EXP-0000, score 99.6580). The kill therefore fires only if a circuit fitted to C1 is essentially no better than a random block against the sealed target. The hypothesis's own section 7.5 prediction says the opposite: the C1-fitted circuit scores 'up to 0.05' against every non-matching construction. Cross-fidelity 0.05 is fifteen times the incumbent's fidelity and maps to a score of 95.0; even cross-fidelity 0.005 maps to 99.5. Both land strictly inside the pre-registered AMBIGUOUS MIDDLE (20.0, 99.558). So the single most likely outcome under 'C1 is the wrong construction' is the middle band, not the kill, and the middle band is by the brief's own words the state where the campaign cannot tell a wrong construction from a wrong start state or a weak fit. That is the non-identifying-failure defect PRIORS has already forced this campaign to repair twice (HYP-0001 objection 2, HYP-0004 objection 2), reappearing as a threshold choice rather than as a mechanism. It is also why the claim 'either reproduces the anchor or delivers the campaign's first MEASURED refutation' is not supported: the third outcome is the one with the highest prior mass.

The threshold is additionally keyed to the wrong quantity. 99.5580 is the incumbent minus the contract's min_improvement of 0.10, which is a PROMOTION bar. Whether a candidate promotes is unrelated to whether a named construction is refuted; borrowing the promotion bar as the identification bar is what puts the kill an order of magnitude past where the discriminating signal lives. Required fix, both parts: (a) set the C1-refutation band from MEASURED quantities, not from the incumbent. Section 7.5 already computes, per surrogate instance, the cross-fidelity of a C1-fitted circuit against each non-matching construction; pre-register the refutation band as score > 100*(1 - F_cross_max) minus a stated margin, with F_cross_max the worst-case measured cross-fidelity over the 7.5 row and the margin justified by the spread across the 3 surrogates. If that computed band overlaps the anchor band the cycle is not decisive and must be said so BEFORE the scored run, not after. (b) Re-draw the section 9 decision table against the new band so that the three rows are exhaustive, non-overlapping, and prescribe DISTINCT actions. As written, the kill row and the ambiguous-middle row both send cycle 7 to C2, which means the table spends a full cycle identically whether or not the kill fires, so the cycle's stated product (one bit that advances the construction pointer) is not actually purchased by the threshold as drawn. State explicitly what the middle band buys that the kill band does not.

(2) THE OFFLINE STUDY THAT SUPPLIES THE KILL THRESHOLD'S PRECONDITION IS PRE-REGISTERED TO EXCEED ITS OWN WALL CLOCK, AND THE BRIEF NEVER SAYS WHICH CELLS THE PRECONDITION NEEDS. Section 7 gives the script 1800 s total. Section 7.4 mandates 3 instances x 7 constructions = 21 fits, each run under the IDENTICAL pinned budget and the IDENTICAL section 5.5 guard, whose per-call ceiling is 90 s. Worst case is 21 x 90 = 1890 s for section 7.4 alone, already over the cap before section 7.1 runs. Section 7.1 is not free either: it must measure t_init at MAXITER 3000 and then, on failure, at 1500, 800 and 400, i.e. roughly 2x a full 3000-iteration init, and the pinning rule only descends the ladder when t_init at 3000 exceeds 37.5 s, so the branch that actually exercises the ladder is the branch that costs the most measurement time. Add 7.2, 7.3, 7.5 and 7.6 and the study is pre-registered to hit its partial-table rule on essentially every execution.

That would be a scheduling annoyance if the table were decoration. It is not: the kill threshold's precondition reads 'the section 7 surrogate self-fit F >= 0.80 measured under the identical pinned budget', and the reopen condition (D) makes a self-fit below 0.80 the thing that voids the verdict. The brief never states which of the 21 cells that precondition quantifies over, never orders the cells, and never says what happens when the missing cells are precisely the ones the precondition names. Meanwhile done_definition item (8) demands 'the 3x7 self-fit F table computed under the identical pinned budget AND the identical wall-clock guard with per-cell guard-fired flags', which directly contradicts section 7's partial-table permission, so the coder is handed two incompatible done-definitions and must exercise judgment in a brief that opens by forbidding it. Required fix, three parts: (a) name the MANDATORY cells, run them FIRST, and state that the kill precondition quantifies over exactly those (the natural choice is the 3 C1-self-fit cells plus the full 7.5 cross-fidelity row, which objection 1's fix also needs); declare the remaining 18 cells explicitly optional and reconcile done_definition (8) with that. (b) Re-price the script's clock from the pinned per-cell cost and state the resulting number, or cut the surrogate count, rather than leaving a 1890 s worst case under an 1800 s cap. (c) Pin N_INITS with margin. Section 5.4's rule is N_INITS = min(6, floor(FIT_BUDGET / t_init)) at FIT_BUDGET = 75.0 s, which saturates the fit budget exactly, while section 5.5 skips remaining inits at 78.0 s and raises StopFit at 85.0 s and calls the guard something that 'MUST NOT FIRE'. Those are incompatible: t_init is measured on ONE coder-generated surrogate, the five scored instances have different conditioning and different convergence behaviour, and a 10 percent slower instance puts the last init past 78 s. Since wall_clock_truncated = true VOIDS every row of section 9 by the brief's own preamble, the pinning rule must leave headroom; use floor(0.6 * FIT_BUDGET / t_init) with the 0.6 stated as the pre-registered margin, or set FIT_BUDGET to 45.0 s against the same 78/85/90 s guard, and justify whichever number you pick from the measured spread of t_init across the 3 surrogates rather than from a single instance.

Fix while revising, non-blocking: (a) section 7.3 says to match 'the sparsity and scale of the payload's terms if visible', which is a judgment call in a brief that forbids judgment calls; state the surrogate generator fully (number of quartets, coupling distribution, seed) and record separately whether the local payload's terms matched it. (b) Section 7.4's 'identical section 5.5 wall-clock guard (same 78/85/90 s thresholds)' must say that t0 is reset per CELL, not per script; as literally written the second cell onward is already past 85 s of script time and every cell after the first would StopFit immediately. (c) Section 5.4's fallback when MAXITER 400 will not fit two inits is 'hand the cycle back as a budget defect for the Director', which is correct, but say where that hand-back is recorded (a question_for_director on the hypothesis record) so it does not surface as a silent no-op. (d) Section 2.1's MARKER branch for terms_absent is right, but note that workspace/baseline.py reads payload['n_qubits'] and scored valid=true at 99.6580, which is positive evidence that the installed scorer does pass a payload with the documented keys; record that in the report so section 0.3's guard does not get read by a later cycle as evidence the keys are absent.

What is NOT objected to, so the revision does not over-correct: committing unconditionally to C1 with the elimination tests demoted to observation-only is exactly the repair HYP-0005 objection 1 demanded, and it is done cleanly; measuring the fit budget first, pinning it as literal constants, and requiring the surrogate to honor the same budget AND the same guard is exactly the repair HYP-0005 objection 2 demanded; the adjoint accumulate-before-undo ordering is now stated unambiguously and the import-time finite-difference check is the right backstop; the element-for-element baseline.block_gates assertion is required, executable, and correctly flagged as assert-not-eyeball; the MARKER at cost 0 with no import of baseline.py remains the correct mechanically distinguishable failure signal; section 2.5 now states an ACTION on the pairwise-overlap table, closing HYP-0005's dangling item; the enumeration covers the encoding axis with a real completeness argument; and section 8's explicit list of what the cycle does not do is good scope discipline. Key the refutation band to the measured cross-fidelity instead of to the promotion bar, and make the offline study's clock and mandatory cells arithmetically consistent with the precondition it feeds, and this is executable. (HYP-0006)
- [OPEN] ELI5: after seven cycles this campaign has still produced zero measured experiments. Cycle 7 did not die on physics or on design: its coder session died because the backend lost subscription access (JOURNAL 2026-09-22 01:21:51Z). The two design flaws the challenger found in cycle 6 are real and are fixed here by DELETING machinery, not adding it. Flaw one was that a bad score could have meant three different things (wrong target construction, wrong start state, or a fit that simply ran out of time) with no way to tell which. Flaw two was a 21-cell offline study pre-registered to exceed its own clock, whose missing cells were exactly the ones the verdict depended on. The fix for both is the same observation: the candidate can measure HOW WELL IT FITTED ITS OWN TARGET, in path, for free, because it built that target itself. That one number separates 'we fitted fine, so the target is wrong' from 'we never fitted, so we learned nothing', and it makes the whole offline surrogate study unnecessary. So: the circuit shape stays byte-identical to the incumbent (6 layers of rz/rx on 8 qubits interleaved with 5 linear CNOT chains, exactly 35 logical CNOTs, not one gate added or moved), the target is built from the payload under ONE named construction committed to unconditionally in advance, the 96 angles are fitted to it under a budget measured and pinned in path with a stated safety margin, and the candidate reports its own achieved self-fit fidelity plus the measured overlaps between the enumerated alternative constructions. Formally: the candidate constructs the sixteen Majorana operators on the 8-qubit register under the shared-qubit (interleaved) Jordan-Wigner encoding E2 (psi^L_j = g_{2j}, psi^R_j = g_{2j+1}, j = 0..3, g_{2k} = Z_0..Z_{k-1} X_k, g_{2k+1} = Z_0..Z_{k-1} Y_k on the single 8-qubit chain), obtains |I> by SOLVING the fermionic annihilation condition (psi^L_j + i psi^R_j)|I> = 0 for all j rather than assuming a kron form, forms T = normalize(expm(-0.5*beta*H_L(E2)) |I>) at beta = 3 with the gauge fix 'largest-modulus amplitude rotated real positive', and fits the incumbent's 96 free angles theta of shape (6,8,2) to T, simulating from |0>^8 by adjoint-gradient L-BFGS-B under a pre-measured, pinned iteration/restart budget with a 0.6 safety margin. The alternative constructions (E1 block encoding, the s = 1.0 normalization, the un-gauge-fixed variant) are enumerated, their pairwise overlaps with T measured and reported, and nothing is eliminated or switched by them: they only set the pre-registered refutation band. The cycle buys exactly one identified bit about one named convention for the price of one cheap run: either the score collapses from 99.658 to roughly 11 and the campaign has reproduced its own sealed F35 anchor to the digit and confirmed the scorer's convention in the same run, or the score stays near 99.6 WITH a demonstrated high self-fit and the campaign has its first MEASURED refutation of a named construction instead of an eighth round of argument. — challenged: Two blocking objections, both arithmetic, both cheap to fix before compute. I verified workspace/baseline.py directly: LAYERS=6, N_CHAINS=5, BLOCK_QUBITS=8, block_angles() returns (6,8,2) from default_rng(0), block_gates() emits rz-then-rx per qubit per layer plus cx(q,q+1) for q=0..6 after layers 0..4, i.e. 131 gates and 35 logical CNOTs. Section 0.1/0.2's byte-identical-topology premise and the mandatory element-for-element assertion are sound and executable exactly as written, and deleting the 21-cell offline surrogate study in favour of an in-path self-fit number is exactly the repair PRIORS HYP-0006 objection 2 demanded. Neither is what I am objecting to.

(1) THE MAJORANA INDEX RANGE IS OFF BY A FACTOR OF TWO, AND THE BRIEF'S OWN MANDATORY ONE-DIMENSIONAL-KERNEL ASSERTION IS THEREFORE PROVABLY GUARANTEED TO FAIL BEFORE ANY FIT RUNS. CONTRACT.md's dev-examples block pins n_majorana = 8 PER SIDE, so the left Majoranas the Hamiltonian uses are psi^L_j for j = 0..7 and a term's 'indices' quartet may name any of 0..7 (the illustrative term is [0,2,3,5]; nothing bounds it below 8). Under E2 that is 8 left plus 8 right = 16 Majoranas on the 8-qubit chain, which is why section 1.1 correctly says g_{2k} for k = 0..7 and the claim text correctly says 'sixteen Majorana operators'. But section 1.1 then pins psi^L_j = g_{2j}, psi^R_j = g_{2j+1} 'for j = 0..3', and section 1.3 forms A_j 'for j = 0..3' and stacks them into a '1024x256' matrix (4 x 256 rows = 1024, which is itself the arithmetic fingerprint of the four-operator version). Two consequences, both fatal in path, neither recoverable by judgment. (a) H_L is undefined: section 1.2 instructs the coder to build sum_t J_t psi^L_i psi^L_j psi^L_k psi^L_l from the term's own indices, and every term containing an index in 4..7 references a psi^L that section 1.1 never defined. A literal-minded executor has no defined behaviour there, in a brief whose preamble forbids judgment calls. (b) The kernel assertion fails deterministically. The brief's own algebra (stated in 1.3 and in the claim) is correct: A_j = g_{2j} + i g_{2j+1} = (Z-string) tensor (X_j + i Y_j) and X + iY = 2|0><1|, so A_j's kernel is exactly the set of states with qubit j in |0>. With only four A_j the common kernel is 2^(8-4) = 16-dimensional, not one-dimensional, so 'assert the kernel is exactly one-dimensional' raises on instance 1 and the cycle produces no scored point, which is the eighth consecutive zero-experiment cycle and trips CONTRACT.md's BUDGET kill that this hypothesis itself invokes. Only the full j = 0..7 set gives the 1-dim kernel |0>^8 that section 1.3, the claim, and report['i_is_vacuum'] all expect. Required fix: set j = 0..7 everywhere (sections 1.1, 1.3, and the stacked matrix becomes 2048x256), state that H_L's index range is 0..7 and assert every term's indices lie in that range, and carry the same correction into section 1.6's E1 row, where the block encoding must place 8 left Majoranas on 4 qubits (JW with k = 0..3 giving g_0..g_7) and 8 right on the other 4, not 4 and 4.

(2) ONE OF THE FOUR ENUMERATED ALTERNATIVES IS GLOBAL-PHASE-EQUIVALENT TO THE COMMITTED TARGET, SO F_cross_max_all IS IDENTICALLY 1.0 AND THE DECISION TABLE LANDS IN NON-DECISIVE ON EVERY POSSIBLE RUN. Section 1.6 member (b) is 'E2 with the gauge fix omitted'. The gauge fix of section 1.5 multiplies T by a unit phase. Fidelity is phase-blind: F_cross(T, b) = |<T | e^{i phi} T>|^2 = 1.0 exactly, on every instance, by construction, independent of the physics. Therefore F_cross_max_all = 1.0 always. Now read the kill table: (K) fires only if F_cross_max_all <= 0.10, so K can NEVER fire; (M) requires score <= 100*(1 - F_cross_max_all) - 0.5 = -0.5, which no score can satisfy, so M can NEVER fire; (N) fires whenever F_cross_max_all > 0.10, so N ALWAYS fires. The cycle is pre-registered to conclude 'claims NOTHING about any construction' before it is run, which voids the hypothesis's entire stated product (one identified bit) and voids the falsifier's case (c), the one the hypothesis calls the informative falsification. This is the PRIORS HYP-0006 objection 1 defect returning in mirror image: last cycle the band was set where no outcome could reach it, this cycle it is set where no outcome can escape it. The same collapse also breaks the table's advertised exhaustive-and-non-overlapping property independently of the phase issue: row (A) is keyed only on score <= 20.0 while row (N) fires on F_cross_max_all > 0.10, so a run that reproduces the anchor at score ~11 with any two alternatives overlapping above 0.10 satisfies A and N simultaneously and prescribes two contradictory next actions. Required fix, three parts: (a) drop member (b) from the overlap table, or replace it with a construction that is not phase-equivalent, and state explicitly that any member differing from T only by a global phase is excluded because the metric is phase-blind (the un-gauge-fixed variant is still worth reporting as a provenance line, just not as a band input). (b) Re-derive F_cross_max_all over the surviving members and state the expected magnitude: E2/s=1.0 versus E2/s=0.5 is exp(-beta H) versus exp(-beta H / 2) applied to the same |I>, which at beta = 3 on a sparse-SYK spectrum are NOT near-orthogonal, so the 0.10 ceiling in row (K) is likely to be violated by the s-variant alone and the band must either be re-priced from the measured number or gated on a different, genuinely near-orthogonal member set (E1 versus E2 is the one that plausibly qualifies). If the computed ceiling cannot be met by any admissible member set, say so BEFORE the scored run and re-key the band, per the same rule that applied last cycle. (c) Re-draw the four rows so they are mutually exclusive by construction: make the F_cross precondition a gate on the whole table (evaluated and reported first) rather than a condition inside three of four rows, and make row (A)'s condition include the same precondition so anchor reproduction cannot co-fire with NON-DECISIVE.

Fix while revising, non-blocking: (a) Section 3.3 picks MAXITER as the FIRST ladder entry with MAXITER * t_iter <= 0.6 * 45.0 = 27.0 s, and section 3.4 then sets N_INITS = floor(27.0 / (MAXITER * t_iter)). Whenever 3000 iterations fit at all (the branch the ladder prefers), that quotient is 1 unless t_iter is below 0.0045 s, so the overwhelmingly likely realized budget is ONE init at 3000 iterations, not the 6 x 3000 the sealed anchor was fitted under and that section 3.5 and CONTRACT.md's BASELINE FAIRNESS HOLD both describe. Init 0 is the deterministic baseline.block_angles() start, which is a random seed-0 draw scoring 0.0034 against the sealed target, so a single L-BFGS-B descent from it is exactly the configuration most likely to stall in a local minimum, drive F_self below 0.90, and land row (N) for budget reasons rather than physics reasons. Either invert the ladder (pick the LARGEST MAXITER such that N_INITS >= 3 also fits) or state a pre-registered minimum N_INITS and let MAXITER descend to honour it; say which quantity has priority. (b) Section 1.1 asserts its encoding is 'exactly the convention in CONTRACT.md's dev-examples block'. It is not: CONTRACT.md pins g_{2k} = Z_0..Z_{k-1} X_k for k = 0..3 with qubit 0 the most significant bit OF THE LEFT REGISTER, i.e. the E1 block split, while E2 spreads k = 0..7 across one 8-qubit chain. E2 may well be the right commitment (the Director's 2026-09-21 23:20Z entry says the scorer uses GIST's Eq. 5 shared-qubit encoding, which is the actual provenance), but citing CONTRACT.md for it hands the coder two contradictory pinned conventions. Cite the journal entry instead and say in one line that CONTRACT.md's dev-examples convention is the E1 alternative, tested only in the section 1.6 table. (c) Section 4.2 puts the diagnostics dict under key 'report' in the RETURNED dict; CONTRACT.md's validator is not documented as tolerant of extra keys, and an unexpected key is a cheap way to score valid=false on all five instances. Prefer the module-level list alone, or state that the returned extra key was verified harmless against the installed scorer. (d) Section 1.4's eigh-versus-expm cross-check is specified 'on the primary instance only'; name which of the five payload 'name' values is the primary so the check is not skipped by ambiguity.

What is NOT objected to, so the revision does not over-correct: measuring the self-fit in path and keying identification to it is the correct and cheap answer to the three-way ambiguity that killed cycles 4 through 6, and deleting the entire offline surrogate study is the right way to satisfy HYP-0006 objection 2 rather than re-pricing a study that never fit its clock; committing unconditionally to one named construction with the alternatives demoted to reported observation remains correct; producing the returned gate list by CALLING baseline.block_gates so the topology is identical by construction rather than by inspection is the right mechanism and closes the assert-not-eyeball item; the accumulate-before-undo adjoint ordering with an import-time finite-difference check on a fixed synthetic target is correct and correctly bounded in cost; the MARKER path at cost 0 with the EXP-0000 note about payload keys is the right mechanically distinguishable failure signal; the three-level wall-clock guard with t0 reset per prepare() call is right and fixes the per-script-versus-per-cell defect of the last brief; and section 5's explicit list of what the cycle does not do is good scope discipline. Correct the Majorana index range so the kernel solve can succeed, and remove the phase-equivalent member so the refutation band is a band the run can actually land in, and this is executable. (HYP-0008)
- [OPEN] ELI5: after eight cycles this campaign has produced zero measured experiments. Every dead cycle argued about the same thing: which exact state the grader is grading us against. This cycle stops re-deriving it, because the campaign's own frozen CONTRACT.md already states the construction in one line and the payload already hands the candidate both ingredients. CONTRACT.md says the sealed target is exp(-beta*H_tot/4) acting on |I>, with H_tot = H_L + H_R; the payload hands us H_tot directly as the Pauli list 'h_cost' and |I> directly as the array 'reference_state'. So the target needs no Majorana algebra, no Jordan-Wigner choice, no kernel solve, and no encoding enumeration: it is two payload keys and one matrix exponential, and every encoding question is already baked into the arrays the scorer itself sealed. That deletion is the whole fix for the index-range defect the challenger found in HYP-0008, because the code that had the wrong index range no longer exists. The circuit shape stays byte-identical to the incumbent (6 layers of rz/rx on 8 qubits interleaved with 5 linear CNOT chains, exactly 35 logical CNOTs, produced by CALLING baseline.block_gates so the topology is identical by construction), and the only thing the candidate does is fit that block's 96 angles to the target from |0>^8 with adjoint-gradient L-BFGS-B under a budget measured in path and pinned with a restart-count floor. Formally: the candidate builds H_tot densely from payload['h_cost'] (leftmost Pauli label character = qubit 0, matching the sealed convention), forms T = normalize(expm(-beta*H_tot/4) @ payload['reference_state']) at beta = 3.0 with the CONTRACT gauge fix applied for provenance only (fidelity is phase-blind, so the gauge fix cannot move the score, and no phase-equivalent variant is used anywhere in the decision table, which is the second challenger fix), fits theta of shape (6,8,2) to T, and returns baseline.block_gates(theta_fit). It also reports, per instance, its own achieved self-fit fidelity F_self against its own committed T, and the cross-overlaps F_cross against three genuinely non-phase-equivalent alternative constructions. Either the score collapses from 99.658 to roughly 9 and the campaign has reproduced its own sealed F35 anchor to the digit while confirming the scorer's convention in the same run, or the score stays high WITH a demonstrated high F_self and the campaign has its first MEASURED, identified refutation of a named convention instead of a ninth round of argument. — challenged: Two blocking objections, both in the decision table, both cheap to fix before compute. What I am NOT objecting to: deleting the Majorana/JW/kernel machinery and taking T from payload['h_cost'] + payload['reference_state'] is the right repair for the HYP-0008 index-range defect and is consistent with CONTRACT.md's frozen Metric line; the three section-6 alternatives are genuinely non-phase-equivalent, so the identically-1.0 collapse is fixed; the accumulate-before-undo adjoint with an import-time finite-difference gate is correct; the restart-floor-first ladder is the right fix for the one-init-at-3000 defect. I verified workspace/baseline.py directly (LAYERS=6, N_CHAINS=5, BLOCK_QUBITS=8, block_angles() -> (6,8,2) from default_rng(0), block_gates() emits rz-then-rx per qubit per layer plus cx(q,q+1) for q=0..6 after layers 0..4, so 131 gates and exactly 35 logical CNOTs, and every gate is rz/rx/cx). Section 0.1, 2 and 3 are executable as written.

(1) THE 0.90 SELF-FIT THRESHOLD IS ABOVE THE ANSATZ'S OWN DEMONSTRATED CEILING, SO ROWS B AND C CANNOT FIRE AND EVERY NON-CONFIRMING OUTCOME LANDS IN NON-DECISIVE. Rows B and C both require min-over-instances F_self >= 0.90. The best fidelity this exact 35-CNOT block is known to reach on the sealed target is F35, and CONTRACT.md's own sealed anchors include 0.8955 (Row 42) and 0.9011 (Row 32), so the minimum over the five instances under the CORRECT construction is 0.8955, which is below 0.90. The hypothesis states this itself: the prediction says F_self lands 'at or above 0.88' and 'within 0.02 of the corresponding sealed F35'. There is no reason a WRONG target is easier for the same 35-CNOT block than the true TFD is, so under a wrong construction min F_self is very likely also below 0.90. Consequence: score > 20 routes to row D (FIT FAILURE, NON-DECISIVE) essentially regardless of whether the target is right or wrong, the identical hypothesis re-runs once, row D fires twice, and the cycle parks for the Director having bought zero identified bits. That is the PRIORS HYP-0006 defect in its original direction: a band set where the informative outcome cannot land. Required fix, pick one and state it: (a) key the fit-succeeded test on a MEASURED capability control rather than an absolute number. Fit the same block, same budget, to a synthetic target that the block can reach EXACTLY (draw theta_ctrl from a pinned rng, set T_ctrl = simulate(block_gates(theta_ctrl)) from |0>^8, fit from the section-4.4 inits) and record F_ctrl. F_ctrl >= 0.99 proves the budget and the optimizer work, and then a low F_self is physics, not budget. That costs one extra fit per process (not per instance) and removes the threshold guess entirely. Or (b) lower the threshold to a number the anchors permit (min F_self >= 0.85) AND state explicitly in the row that the comparison is against the anchor level, not against 1.0. Whichever you pick, the prediction section must be made consistent with the row: as written it predicts a value that fails its own row.

(2) ROW A CAN FALSE-CONFIRM A WRONG CONSTRUCTION, AND THE RUN ALREADY CARRIES THE NUMBER THAT WOULD MAKE IT EXACT. Row A is keyed only on score <= 20.0, i.e. mean F >= 0.80, and the table says F_cross is 'never a gate on rows A or D'. Section 6 exists precisely because an alternative may overlap the committed T heavily (that is what row C anticipates). If, say, m2 = normalize(exp(-beta*H_tot/8)|I>) has F_cross ~ 0.9 with the sealed target, then fitting the wrong one of the two can still score inside row A's window, and the cycle records 'the construction and the start state are confirmed together, the sealed F35 anchor is reproduced to the digit' when it was not. The fix is free because the cycle produces both halves of an exact identity: if the scorer's target equals the candidate's committed T, then the blinded per-instance s_i must equal 100*(1 - F_self_i) to within optimizer noise, because both are the fidelity of the SAME emitted circuit against the SAME state. Re-key row A as: score <= 20.0 AND |s_i - 100*(1 - F_self_i)| <= 1.0 on every instance. Add the mismatch case as its own row (score <= 20.0 with the agreement test failing means the scorer grades a state that is close to but not equal to T, which is a partial-overlap finding and is cycle 10's starting point, not a confirmation). This also makes row A's verdict robust to the loose 20.0 cutoff, so that cutoff does not need to move. Note in the brief that per-instance s_i must be read from the scorekeeper verdict, and if only the mean is returned apply the same test to the mean.

REQUIRED FIX, cheap and not arguable: sections 0.1 and 5 depend on `import baseline` resolving from candidate.py at score time, and that is UNVERIFIED. I checked: workspace/incumbent.py is a self-contained 40-line copy of the block and does NOT import baseline, so the fact that the incumbent scored 99.6580 is NOT evidence that a candidate module can import a sibling file. An ImportError produces valid=false on all five instances and the cycle buys nothing. Make the import path explicit: `import os, sys; sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))` before `import baseline`, and on ImportError fall back to an inlined verbatim copy of block_angles/block_gates with the section-0.1 element-for-element assertion run against that copy instead, recording `baseline_import='inlined_fallback'` in DIAGNOSTICS.

Non-blocking, fix while revising: (a) reopen_condition clause (3) is keyed on a check the brief never orders. Add to section 6: record `n_h_cost = len(payload['h_cost'])`, `K = payload['K']`, and `h_cost_count_ok = (n_h_cost == 2*K)`. A reopen condition that depends on an unrecorded diagnostic is not a reopen condition. (b) Section 4.1 sets TOTAL_BUDGET_S = 900 and PER_INSTANCE_CAP_S = 150 with no stated provenance; state where the 900 s comes from (the scorer's per-candidate wall clock, if known, else say it is a self-imposed cap and that exceeding it is a scorer-side risk you accepted). (c) Section 1.3's expm cross-check is gated on `payload['name'] == 'INST-A'`; if the installed scorer passes a different name string, the check is silently skipped. Make it run on the FIRST prepare call of the process regardless of name, and record the observed name. (d) Section 4.4 caps N_INITS at 6 but init 0 is the deterministic seed-0 draw that scores 0.0034 against the sealed target; record F per init so a row-D verdict can be read against how many inits actually converged, not just how many ran. (HYP-0009)
- [OPEN] ELI5: after nine cycles this campaign has produced zero measured experiments. Nothing has been wrong with the physics; every cycle has died on bookkeeping. Cycle 9's design was one fix away from launching: the challenger found that (1) its success test could have declared victory on a state that was merely CLOSE to the graded one rather than equal to it, and (2) it assumed the candidate file can import its neighbour file at score time, which nobody had checked. Both fixes are free and are made here. The success test now uses an exact identity the run itself produces: if the state we aim at is the state the grader grades, then the grader's per-instance penalty must equal 100*(1 - our own measured self-fit fidelity) on that same emitted circuit, because both numbers are the fidelity of the SAME circuit against the SAME state. If those two numbers disagree, we are close but not equal, and that is a different, separately recorded finding rather than a false confirmation. The import is made explicit, with a verbatim inlined copy as the fallback, so an ImportError can no longer silently zero the cycle. Everything else stays as cycle 9 had it: the circuit shape is byte-identical to the incumbent (6 layers of rz/rx on 8 qubits interleaved with 5 linear CNOT chains, exactly 35 logical CNOTs, produced by calling the incumbent's own gate builder), and the only thing the candidate does is build the target from two payload keys and fit that block's 96 angles to it. Formally: the candidate builds H_tot densely from payload['h_cost'] (leftmost Pauli label character = qubit 0, matching the sealed convention), commits unconditionally to T = normalize(expm(-beta*H_tot/4) @ payload['reference_state']) at beta = 3.0, which is CONTRACT.md's frozen Metric line read literally and therefore needs no Majorana algebra, no Jordan-Wigner choice, no kernel solve and no encoding enumeration, fits theta of shape (6,8,2) to T from |0>^8 with adjoint-gradient L-BFGS-B under a budget measured in path and pinned with a restart-count floor, returns baseline.block_gates(theta_fit), and reports per instance its own achieved self-fit fidelity F_self against its own committed T, the per-init fidelity table, and the cross-overlaps F_cross against three genuinely non-phase-equivalent alternative constructions. Either the score collapses from 99.658 to roughly 9 AND the agreement test |s_i - 100*(1 - F_self_i)| <= 1.0 holds on every instance, in which case the campaign has reproduced its own sealed F35 anchor to the digit and confirmed the scorer's target construction and start state together in one run, or the score stays high WITH a demonstrated high F_self, in which case the campaign has its first MEASURED, identified refutation of a named construction instead of a tenth round of argument. — challenged: Two blocking objections, both cheap, neither about the physics. What I am NOT objecting to: taking T from payload['h_cost'] + payload['reference_state'] per CONTRACT.md's frozen Metric line is right; the agreement test |s_i - 100*(1-F_self_i)| <= 1.0 is the correct repair for HYP-0009 objection 1 and is an exact identity at C2=35; the explicit sys.path insert plus a verbatim inlined fallback is the correct repair for objection 2, and I verified the fallback body is a faithful transcription of workspace/baseline.py (LAYERS=6, N_CHAINS=5, BLOCK_QUBITS=8, ANGLE_SEED=0, rz-then-rx per qubit per layer, cx(q,q+1) for q=0..6 after layers 0..4, 131 gates, exactly 35 cx). The import-time topology assertion, the adjoint FD gate, the restart floor and the per-init table are all executable as written.

BLOCKING 1: THE EMITTED CIRCUIT IS MISSING THE 8-HADAMARD REFERENCE PREP THAT CONTRACT.md ASSIGNS TO THIS EXACT BLOCK, SO A FIDELITY SHORTFALL WILL NOT BE IDENTIFIED. CONTRACT.md states it twice, verbatim: 'every candidate prepares its own reference: the 35-CNOT baseline via 8 Hadamards to |+>^8' (Objective) and 'prepare your own reference: 8 H for the hardware-efficient block' (Dev examples). The sealed anchors F35 (0.8955 / 0.9164 / 0.9011 / 0.9228 / 0.9229) were refit under that convention, i.e. for the circuit H^8 then block(theta) simulated from |0>^8. I checked workspace/baseline.py and workspace/incumbent.py directly: NEITHER emits any 'h' gate. block_gates(theta) starts with rz on |0>, which is a global phase per qubit, so 8 of the 96 parameters are dead and the layer-0 single-qubit states are confined to the rx great circle through |0>, whereas the H-prefixed family covers the full sphere and strictly contains the no-H family per qubit. This cycle therefore fits a STRICTLY WEAKER ansatz than the one the anchor was measured on, at identical logical cost (8 Hadamards are 1q gates, C2 stays exactly 35, so the fix is free on the scored axis). The damage is to identification, which is the entire purpose of the cycle: if the committed T is in fact the sealed target but the missing prefix costs, say, 0.1 in fidelity, the score lands near 20-30 with F_self matching it exactly, the agreement test PASSES (it compares the same circuit against the same T and cannot see an ansatz-power deficit), and the campaign records neither a confirmation nor the pre-registered refutation. Worse, if the shortfall pushes F_self below 0.85 the kill_threshold's own language routes the verdict to 'fitting failure, repair is more restarts', which is the wrong repair for a missing gate prefix and would burn the next cycle. Required fix, all of it mechanical: emit gates = [{'name':'h','qubits':[q],'param':0.0} for q in 0..7] + block_gates(theta_fit); fit theta against that same prefixed circuit (the forward sweep and the adjoint both start after the 8 H, which are parameter-free); change section 5's assertion to len(gates) == 139 with exactly 35 cx and 8 h and no other two-qubit gate; and extend section 0.1's element-for-element assertion to the prefixed schedule while still asserting the 131-gate block substring unchanged. Restate the claim's 'byte-identical to the incumbent' as 'the incumbent's 35-CNOT block topology unchanged, under CONTRACT.md's stated 8-Hadamard reference prep' so the pre-registration names the circuit that is actually run. If the researcher believes the sealed F35 was fit WITHOUT the prefix, that belief contradicts the frozen contract text and must go to the Director as a question, not into a run.

BLOCKING 2: THE MOST LIKELY OUTCOME BAND HAS NO PRE-REGISTERED VERDICT. The decision surface is keyed at score <= 20.0 (confirmation / agreement-mismatch) and score >= 90.0 (falsifier and kill). The entire interval 20.0 < score < 90.0 is unassigned in the prediction, the falsifier AND the kill_threshold, for both F_self branches. That band is not a corner case: it is exactly what a partially-correct target construction produces (a wrong beta factor, |I> versus a near neighbour, a construction overlapping the sealed target at 0.5-0.9), and objection 1 independently pushes the outcome into it. A hypothesis whose likeliest result maps to no recorded verdict cannot be read after the fact without re-arguing, which is how the last five cycles were spent. Required fix: make the verdict table exhaustive over (score, F_self) before the run. Concretely, add as pre-registered rows: (i) 20.0 < score < 90.0 with F_self >= 0.85 and the agreement test passing = PARTIAL-OVERLAP, a measured statement that the sealed target is at overlap approximately 1 - score/100 with the committed T, with the F_cross table naming the near neighbour, and state explicitly whether this reopens under reopen_condition clause (2) (which today is keyed only on score >= 90.0 and so would not fire); (ii) 20.0 < score < 90.0 with F_self >= 0.85 and the agreement test FAILING, stating which of the two numbers is trusted and what the next cycle does; (iii) any band with F_self < 0.85 = fitting failure with the per-init table deciding restarts versus optimizer, unchanged. Keep the numeric cutoffs where they are; only the coverage is missing.

Non-blocking, fix while revising: (a) section 0.1's snippet is introduced as 'At module top, exactly:' and begins with 'import os, sys' while the fallback bodies use np; a literal-minded executor will place it above 'import numpy as np' and, on the fallback branch, raise NameError at the first block_angles() call. Put 'import numpy as np' explicitly as the first line of that snippet. (b) Section 3.1's MAXITER formula divides the per-instance budget by 6 * median_eval_s, treating one L-BFGS-B iteration as one objective+gradient evaluation; with line search nfev typically exceeds nit by 1.2-2x, so the realised wall time can be roughly double the budgeted 0.6 * PER_INSTANCE_CAP_S. Either divide by 6 * 2 * median_eval_s or state that the 0.6 margin is intended to absorb it. (c) Prediction and done_definition both hard-code 131 gates / C2=35; both need the objection-1 update to 139 gates / C2=35 so the done_definition does not fail the circuit the brief asks for. (HYP-0010)
- [OPEN] ELI5: after ten cycles this campaign has produced zero measured experiments. Cycle 10 was one free fix from launching. The challenger found that the circuit we were going to emit was missing its opening move: the campaign's own contract says, twice and verbatim, that this particular 35-gate block is always run AFTER eight Hadamard gates (the move that puts each of the eight qubits into an equal mix of 0 and 1), and the 89% anchor number we are trying to reproduce was measured on the Hadamard-prefixed circuit. Without the prefix the first layer of angles is partly dead and the circuit family we fit inside is strictly smaller, so a fidelity shortfall could not have been told apart from a wrong target. Hadamards are one-qubit gates, so adding them costs exactly zero on the scored cost axis: the fix is free. The second finding was that the most likely outcome, a score somewhere between 20 and 90, had no pre-registered verdict at all, which is precisely how the last five cycles turned into arguments after the fact. This cycle fixes both by adding the eight Hadamards and by writing a verdict table that covers every possible (score, self-fit) pair in advance. Everything else is cycle 10 unchanged: the 35-CNOT block topology is byte-identical to the incumbent's (6 layers of rz/rx on 8 qubits interleaved with 5 linear CNOT chains, produced by calling the incumbent's own gate builder), the target is built from two payload keys under one construction committed to unconditionally, and the only work is fitting 96 angles to it. Formally: the candidate builds H_tot densely from payload['h_cost'] (leftmost Pauli label character = qubit 0, matching the sealed convention), commits unconditionally to T = normalize(expm(-beta*H_tot/4) @ payload['reference_state']) at beta = 3.0, which is CONTRACT.md's frozen Metric line read literally and therefore needs no Majorana algebra, no Jordan-Wigner choice, no kernel solve and no encoding enumeration, fits theta of shape (6,8,2) to T by simulating the FULL emitted circuit H^{\otimes 8} then block_gates(theta) from |0>^8 with an adjoint-gradient L-BFGS-B under a budget measured in path and pinned with a restart-count floor, emits [8 x h] + baseline.block_gates(theta_fit) (139 gates, exactly 35 logical CNOTs, C2 = 35 unchanged), and reports per instance its own achieved self-fit fidelity F_self against its own committed T, the per-init fidelity table, and the cross-overlaps F_cross against three genuinely non-phase-equivalent alternative constructions. The verdict is then read off a table that is exhaustive over (score, F_self, agreement test) and written before the run, so every possible result is a recorded measurement rather than a new round of argument. — challenged: Two blocking objections, both in the post-run inference machinery, both free to fix. What I am NOT objecting to: the 8-Hadamard prefix is correct and well-sourced. I verified CONTRACT.md carries it twice verbatim (line 3: 'the 35-CNOT baseline via 8 Hadamards to |+>^8'; line 36: 'prepare your own reference: 8 H for the hardware-efficient block'), and I verified workspace/baseline.py contains no 'h' gate anywhere (LAYERS=6, N_CHAINS=5, BLOCK_QUBITS=8, ANGLE_SEED=0, rz-then-rx per qubit per layer, cx(q,q+1) for q=0..6 after layers 0..4, 131 gates, exactly 35 cx, and prepare() returns the bare block), so the prefix is genuinely missing today and adding it costs 0 on the C2 axis. The 139-gate topology assertion, the accumulate-before-undo adjoint with the FD gate from the cached |+>^8, the restart-floor-first ladder with the 2.0x nfev factor, and committing unconditionally to T = normalize(expm(-beta*H_tot/4)|I>) are all sound and executable as written.

OBJECTION 1 (BLOCKING): F_cross is measured against the wrong reference, so the identification machinery the cycle sells as its payoff is arithmetically inoperative, and it contradicts the brief's own agreement test. Section 6 records F_cross_j = |<T|m_j>|^2, i.e. overlaps of the committed T with the alternatives. reopen_condition clause (2) and section 8 ROWs B/C then identify the sealed state by matching F_cross_j against 'the state implied by the measured score (|<T|sealed>|^2 approximately 1 - score/100)'. That identity is false. 1 - S/100 is F(psi_emitted, sealed), not |<T|sealed>|^2, and psi_emitted differs from T by the ansatz deficit: the brief's own prediction is F_self ~ 0.89. So in the SUCCESS case, sealed == T exactly, 1 - S/100 = 0.89, and clause (2) would conclude the sealed state has 0.89 overlap with T, i.e. that T is not the sealed target, while the agreement test in section 4 (|s_i - 100*(1 - F_self_i)| <= 1.0, which is the correct device because both sides are the same circuit against the same state) would correctly PASS. Two pre-registered devices reading the same two numbers and returning opposite verdicts is exactly the after-the-fact argument this cycle exists to prevent. In the failure case it is worse: if sealed = m2 with |<T|m2>|^2 = 0.95, the measured 1 - S/100 is depressed by the ansatz deficit relative to m2 (which need not be ~0.89 at all, since theta was fitted to T, not to m2), so the '>= 0.99 match' test essentially never fires and ROWs B/C hand cycle 12 nothing. Required before compute (free, same simulation the run already does): report, per instance, F_emit_j = |<m_j|psi_emitted>|^2 for j = 1,2,3 on the SAME re-simulated emitted circuit of section 4, alongside F_self (which is F_emit against T). Those four numbers are all fidelities of one fixed circuit against candidate states and are therefore directly comparable to the scorer's 1 - s_i/100. Rewrite reopen clause (2) and ROWs B/C to identify the near neighbour by |F_emit_j - (1 - s_i/100)| being smallest and within tolerance, and delete the '|<T|sealed>|^2 approximately 1 - score/100' sentence. Keep |<T|m_j>|^2 if you like, but label it as a construction-distance diagnostic that plays no part in identification.

OBJECTION 2 (BLOCKING): ROW F's 0.85 floor is mis-calibrated against the ansatz ceiling and, because it is checked FIRST and 'overrides every other row', it converts the cycle's two most likely informative outcomes into 'buys nothing'. (a) Ceiling side: the sealed anchor on the primary instance is F35 = 0.8955, which IS the fitted maximum of this exact 96-angle topology. The ROW F floor sits 0.045 below it. A merely mediocre multi-start landing at F_self = 0.84 on one instance fires ROW F and voids the cycle even when the agreement test passes to 1e-3 and the score is ~16, i.e. even when the construction is confirmed. Fmin over five instances makes this worse, not better: the row triggers on the single worst instance. (b) Wrong-T side: if the committed T is not the sealed state, there is no reason the 35-CNOT block reaches 0.85 against it. The anchor 0.8955 is the ansatz maximum against the CORRECT TFD; against a different 256-dim state the fitted maximum is unbounded below and could easily be 0.5-0.7. So the single most likely wrong-construction signature is high score WITH low F_self, which ROW F classifies as a fitting failure that 'touches' the construction question not at all, and ROW E (the only DEAD row) is gated on Fmin >= 0.85 and therefore cannot fire in the very scenario kill_threshold was written for. Required before compute: (i) split ROW F on optimizer evidence rather than on F_self alone. Low F_self with all N_INITS runs at scipy status 0 and ||grad||_inf <= 1e-6, clustered within a stated spread, is a CONVERGED ANSATZ-CEILING measurement against T and is a physics verdict (it enters ROWs A-E with F_self read as the ceiling, and combined with the F_emit_j table from objection 1 it identifies the near neighbour); only a low F_self accompanied by status 1 / maxiter / large gradient norms, or a wide spread across inits, is the non-verdict FITTING FAILURE. (ii) Re-derive the floor from the ceiling rather than pinning a round number: state it as F_self >= F35_expected - delta with delta pre-registered (e.g. 0.03) per instance against the contract's sealed anchors, which are already public in CONTRACT.md, so the floor cannot sit within noise of the achievable maximum. (iii) Restate kill_threshold and the falsifier in terms of the revised rows so the DEAD condition is reachable.

NON-BLOCKING (fix while you are in there, no re-challenge needed): section 3's random inits use default_rng(1000 + instance_index*100 + k) but 'instance_index' is never defined anywhere in the brief. payload carries 'name' (INST-A..E), not an index. For a brief that claims zero judgment calls, pin it explicitly, e.g. instance_index = {'INST-A':0,'INST-B':1,'INST-C':2,'INST-D':3,'INST-E':4}[payload['name']] with a KeyError fallback to a module-level call counter, and record the resolved value in DIAGNOSTICS. (HYP-0011)
- [OPEN] ELI5: after twelve cycles this campaign has still produced zero measured experiments. Cycle 12 did not die on a design flaw: HYP-0012 was pre-registered with both of cycle 11's fixes applied and was never executed, so there is no challenge record, no candidate file and no score. The campaign's failure mode is no longer physics and no longer inference rules, it is simply that no run has ever reached the scorer. This cycle therefore changes NOTHING about the science and adds exactly one thing: a guarantee that the run produces a scored verdict no matter what happens inside it. The design is HYP-0012 unchanged, restated in full so it is standalone: build the target from two payload keys, commit to it unconditionally, keep the circuit shape byte-identical to the incumbent's 35-CNOT block with the contract-mandated 8-Hadamard prefix (one-qubit gates, so the two-qubit cost stays exactly 35 and the cost penalty stays zero), fit the 96 angles to the target with an adjoint-gradient optimizer under a pinned restart floor, and read the verdict off a table written before the run that is exhaustive over (score, agreement band, convergence). The single addition is an execution guard: the multistart is deadline-aware, and any exception or budget overrun anywhere in the fit still returns a valid 139-gate circuit built from the incumbent's own seed-0 angles, with the failure recorded in the diagnostics. A cycle that crashes teaches nothing; a cycle that returns the incumbent circuit with a recorded reason at least keeps the ledger honest and costs one scored run. Formally: the candidate builds H_tot densely from payload['h_cost'] (leftmost Pauli label character = qubit 0, matching the sealed convention), commits unconditionally to T = normalize(expm(-beta*H_tot/4) @ payload['reference_state']) at beta = 3.0, which is CONTRACT.md's frozen Metric line read literally and therefore needs no Majorana algebra, no Jordan-Wigner choice, no kernel solve and no encoding enumeration, fits theta of shape (6,8,2) to T by simulating the FULL emitted circuit H^{\otimes 8} then baseline.block_gates(theta) from |0>^8 with an adjoint-gradient L-BFGS-B, emits [8 x h] + baseline.block_gates(theta_fit) (139 gates, exactly 35 logical CNOTs), and reports per instance its achieved self-fit fidelity F_self against its own committed T, the per-init (fidelity, status, nit, nfev, gradient-infinity-norm) table, the derived convergence flags, the cross-overlaps against three genuinely non-phase-equivalent alternative constructions, and the guard's status. — challenged (advisory: proceeding to coder): Two blocking objections, both cheap, neither about the physics or the target construction. What I am NOT objecting to: HYP-0012 was pre-registered and never executed, so restating it in full is not a duplicate; T = normalize(expm(-beta*H_tot/4) @ payload['reference_state']) is CONTRACT.md's frozen Metric line read literally; the 8-Hadamard prefix is contract-mandated twice verbatim and costs 0 on the C2 axis; the exhaustive (S, A, C, G) verdict table and the no-absolute-fidelity-threshold convergence flags are the right repairs for HYP-0011. I verified workspace/baseline.py directly (LAYERS=6, N_CHAINS=5, BLOCK_QUBITS=8, ANGLE_SEED=0, block_angles() -> (6,8,2) from default_rng(0), rz-then-rx per qubit per layer, cx(q,q+1) for q=0..6 after layers 0..4, 131 gates, exactly 35 cx, no 'h' anywhere); sections 0.1, 0.2 and 5 are executable exactly as written.

OBJECTION 1 (BLOCKING, arithmetic): the pre-registered adjoint seed vector in section 2.3 is wrong by a conjugation AND a factor of 2, and the brief simultaneously forbids judgment calls and mandates an import-time gate that will reject it. Let c = <T|psi>, f = 1 - |c|^2, so df/dtheta_k = -2*Re( conj(c) * <T|dpsi/dtheta_k> ). The brief specifies lam = -2*conj(c)*T with grad_k = 2*Re(<lam|dpsi/dtheta_k>). Expanding that literally: conj(lam_i) = -2*c*conj(T_i), so <lam|dpsi> = -2*c*<T|dpsi>, and 2*Re(<lam|dpsi>) = -4*Re( c * <T|dpsi> ). That is the correct quantity with c in place of conj(c) (not equal in general, since Re(c*x) != Re(conj(c)*x)) and twice too large. The consistent pair is lam = -2*c*T with grad_k = Re(<lam|dpsi/dtheta_k>) (equivalently lam = -c*T with grad_k = 2*Re(<lam|dpsi>)). As written, the mandatory section-2.3 finite-difference gate raises RuntimeError at import, so the done_definition ('passes all four import-time gates with no exception') is unsatisfiable by the literal brief, and a Codex-grade executor told to make zero judgment calls has to silently invent the fix. This is exactly the class of defect that killed HYP-0005/0006/0008 as arithmetic. FIX: replace the section-2.3 formula with lam = -2*<T|psi>*T and grad_k = Re(<lam| dpsi/dtheta_k>), state that the FD gate is the arbiter of the convention, and keep everything else in 2.3 unchanged.

OBJECTION 2 (BLOCKING, inference machinery): the section-10 budget path conflates a self-imposed clock overrun with an execution failure, and ROW G then discards the very verdict this cycle exists to buy. Section 10 sets GUARD_FIRED = True with guard_reason 'budget_overrun:<seconds>' while returning the FITTED circuit; section 8 ROW G is checked FIRST and overrides everything, declaring 'EXECUTION FAILURE, not a physics verdict. Nothing is confirmed or refuted' at any S, A, C. So a run that returns the fitted circuit, scores 9.0, satisfies AGREE_EXACT and has FIT_CONVERGED true on all five instances is recorded as buying nothing, purely because wall time crossed 150 s. PER_INSTANCE_CAP_S = 150 is, by the brief's own section 3.2, a SELF-IMPOSED number with no documented relation to any scorer limit, and the overrun is not remote: MAXITER is sized by the estimated nfev/nit factor 2.0 and is floored at 400, so if the measured median_eval_s exceeds about 0.031 s the floored budget alone (6 inits x 400 nit x 2.0 x median_eval_s) exceeds the cap before the fit starts, and any real nfev/nit ratio above 2.0 does the same. ROW G's own stated expectation ('S ~99.658 on the guarded instances, since the guard emits the incumbent's seed-0 angles') is also false on this path, since the fitted circuit is returned, and it is false on the exception path too: the guard circuit is H_PREFIX + seed-0 block, which is NOT the incumbent circuit (baseline.prepare returns the bare block with no Hadamards), so its score is unmeasured, not 99.658. FIX, all free: (a) split the flag, GUARD_FIRED true only on the exception path where the seed-0 guard circuit is actually emitted, and a separate recorded boolean BUDGET_OVERRUN (with its seconds) on the fitted path; (b) make ROW G key on GUARD_FIRED alone; (c) add one sentence to ROW A/B/C/D that BUDGET_OVERRUN true is recorded as a scorer-side timeout risk and does not change the row, since the emitted circuit and F_self are unaffected by wall time; (d) drop or correct ROW G's '~99.658' expectation, stating instead that the guard circuit's score is unmeasured. (HYP-0013)
- [ALIVE] ELI5: after twelve cycles this campaign has still produced zero measured experiments. Cycle 12 did not die on a design flaw: HYP-0012 was pre-registered with both of cycle 11's fixes applied and was never executed, so there is no challenge record, no candidate file and no score. The campaign's failure mode is no longer physics and no longer inference rules, it is simply that no run has ever reached the scorer. This cycle therefore changes NOTHING about the science and adds exactly one thing: a guarantee that the run produces a scored verdict no matter what happens inside it. The design is HYP-0012 unchanged, restated in full so it is standalone: build the target from two payload keys, commit to it unconditionally, keep the circuit shape byte-identical to the incumbent's 35-CNOT block with the contract-mandated 8-Hadamard prefix (one-qubit gates, so the two-qubit cost stays exactly 35 and the cost penalty stays zero), fit the 96 angles to the target with an adjoint-gradient optimizer under a pinned restart floor, and read the verdict off a table written before the run that is exhaustive over (score, agreement band, convergence). The single addition is an execution guard: the multistart is deadline-aware, and any exception or budget overrun anywhere in the fit still returns a valid 139-gate circuit built from the incumbent's own seed-0 angles, with the failure recorded in the diagnostics. A cycle that crashes teaches nothing; a cycle that returns the incumbent circuit with a recorded reason at least keeps the ledger honest and costs one scored run. Formally: the candidate builds H_tot densely from payload['h_cost'] (leftmost Pauli label character = qubit 0, matching the sealed convention), commits unconditionally to T = normalize(expm(-beta*H_tot/4) @ payload['reference_state']) at beta = 3.0, which is CONTRACT.md's frozen Metric line read literally and therefore needs no Majorana algebra, no Jordan-Wigner choice, no kernel solve and no encoding enumeration, fits theta of shape (6,8,2) to T by simulating the FULL emitted circuit H^{\otimes 8} then baseline.block_gates(theta) from |0>^8 with an adjoint-gradient L-BFGS-B, emits [8 x h] + baseline.block_gates(theta_fit) (139 gates, exactly 35 logical CNOTs), and reports per instance its achieved self-fit fidelity F_self against its own committed T, the per-init (fidelity, status, nit, nfev, gradient-infinity-norm) table, the derived convergence flags, the cross-overlaps against three genuinely non-phase-equivalent alternative constructions, and the guard's status. (Δ=-90.8089) — mechanism: The 99.66-point baseline was almost entirely an angle-fitting deficit, not a structural or physics deficit: the incumbent emitted the 35-CNOT block at seed-0 angles, which are instance-blind, so its fidelity sat near 0.0 (EXP-0013)
- [OPEN] ELI5: the grader changed its mind about what counts as winning, and almost nothing in the campaign has caught up to that yet. Until now the goal was 'match the 89-92% reference state as closely as you can', and cycle 13 finally did it: it reproduced the campaign's own sealed anchor fidelity to about two ten-thousandths, which also proved (measured, not argued) that the target construction taken straight from the contract's Metric line is the right one. The new goal is different and harder to fake: reaching the anchor fidelity is now worth zero credit by itself, and the only thing scored is HOW CHEAP the circuit is that reaches it. Cost here means the number of two-qubit entangling gates, the expensive ones on real hardware. The reference circuit spends exactly 35 of them. So the question this cycle asks is the simplest possible version of the campaign's actual scientific question: are all 35 of those gates actually doing work, or is the reference wasteful? The way to find out is to delete them one at a time. Take the reference circuit, try removing each single two-qubit gate, re-tune the 96 rotation knobs around the hole, and keep the deletion that hurts least. If the re-tuned circuit still reaches anchor fidelity, the gate was waste: accept the deletion and try again on what is left. Stop the first time no single remaining gate can be removed without dropping below the anchor. The number of gates left at that moment IS the answer the contract asks for, the cheapest preparation that still reaches anchor fidelity, and the trace of (cost, fidelity) pairs collected on the way down IS the fidelity-vs-cost curve the contract wants recorded. Why this and not the paper's ma-QAOA + SAP/OSAP route first: I priced that route analytically before proposing anything, and it does not fit through the door yet. Each of the 2K = 24 four-body Pauli strings in H_cost costs 6 CNOTs to exponentiate (2(w-1) for weight w = 4), so ONE ma-QAOA layer costs about 144 two-qubit gates plus 1 for the |I> reference. The contract declares anything above 70 INVALID, so an un-pruned p = 1 ma-QAOA candidate cannot even be scored: it needs roughly 80% of its terms pruned away just to become a legal submission, and roughly 96% pruned (about 5 surviving four-body terms) to land under 35. That is a real and interesting measurement, but it is a large search and it must not be the first thing attempted under a metric no candidate has ever been scored against. This cycle buys the cheap, decisive bit first: whether the 35-gate anchor circuit is itself compressible, on the one structure family the campaign has already validated end to end. Formally: branching from EXP-0013, the candidate rebuilds the target unchanged (H_tot dense from payload['h_cost'] with leftmost Pauli label character = qubit 0, T = normalize(expm(-beta*H_tot/4) @ payload['reference_state']) at beta = payload['beta'], which is CONTRACT.md's frozen Metric line read literally and needs no Majorana algebra, no Jordan-Wigner choice and no kernel solve), then runs greedy backward elimination over a boolean mask on the 35 CNOT slots of the hardware-efficient block (6 layers of rz/rx on 8 qubits interleaved with 5 linear chains cx(q,q+1), q = 0..6, after layers 0..4), with the contract-mandated 8-Hadamard prefix that costs 0 on the two-qubit axis, refitting theta of shape (6,8,2) by adjoint-gradient L-BFGS-B at every tentative removal, accepting a removal only when the refit self-fit fidelity clears the CONTRACT-published per-instance anchor F35 plus a 0.002 margin, under a wall-clock deadline guard that always returns a valid circuit. The structural change is deliberate and is the whole hypothesis: the incumbent's byte-identical-topology constraint, correct for confirming the convention, is exactly what the new metric makes worthless, so it is dropped here for the first time. — challenged (advisory: proceeding to coder): Two blocking objections. Neither is about the physics, the target construction, or the choice of greedy pruning as the cheap first bit.

What I am NOT objecting to (verified): workspace/baseline.py is exactly as Section 2.1 transcribes it (LAYERS=6, N_CHAINS=5, BLOCK_QUBITS=8, ANGLE_SEED=0, block_angles() -> (6,8,2) from default_rng(0), rz-then-rx per qubit per layer, cx(q,q+1) for q=0..6 after layers 0..4, 131 gates, exactly 35 cx, no 'h'), so the slot indexing s = 7*layer + q and the Section 2.3 element-for-element gate are executable as written. The ANCHOR table is transcribed correctly from CONTRACT.md's public Metric paragraph and is not battery access. Reusing EXP-0013's target, simulator and adjoint verbatim is right, and requiring the FD gate to run under a 20-slot NON-full mask is the correct new check. The ma-QAOA deferral is priced correctly: 2K = 24 weight-4 strings x 2(w-1) = 6 CNOTs = 144, +1 for |I>, i.e. 145 > 70, so an un-pruned p=1 candidate is INVALID by the contract's own cap. Deprioritising it is justified.

OBJECTION 1 (BLOCKING, arithmetic + inference): the 240 s deadline cannot pay for the search, and the deadline branch has no pre-registered verdict, yet it is the branch that will fire, and it can auto-ship the campaign on a truncated search.

Budget, priced from this campaign's own measured constants. EXP-0013 ran inside PER_INSTANCE_CAP_S = 150.0 with N_INITS = 6 and MAXITER = clip(int(0.6*150/(6*2*MEDIAN_EVAL_S)), 400, 3000); by construction that formula spends about 0.6*150 = 90 s on 6 inits x MAXITER iterations on the same 139-gate, 256-dimensional circuit and the same adjoint. Take the most optimistic case, MAXITER clipped at its 3000 ceiling, i.e. 18000 iterations in ~90 s (~5 ms per iteration). Then: Section 5.2's full-structure fit is 8 x 3000 = 24000 iterations = ~120 s, half the entire 240 s budget before one tentative refit runs. Round 1 (Section 6.2) is 35 x 400 = 14000 iterations = ~70 s. Those two alone are ~190 s of 240. Reaching the predicted crossing at cost 28 needs rounds 35+34+...+29 = 224 tentative refits = ~90000 iterations = ~450 s, plus 7 consolidations at 4 x 1500 = 6000 iterations each = another ~210 s. Total ~780 s against a 240 s cap: a 3-4x overrun at the optimistic end. If MEDIAN_EVAL_S is large enough that EXP-0013's formula picked MAXITER below 3000, Section 5.2's hard-coded 8 x 3000 overruns 240 s on the baseline fit alone and the cycle emits the full-structure circuit with stop_reason unset.

The overrun is not merely a slow run, it corrupts the verdict three ways. (a) Section 7.2 breaks mid-round on deadline, but done_definition (5) requires the COMPLETE 35-entry round1_table and the falsifier's converged-vs-under-optimised distinction is read off exactly that table: a deadline hit inside round 1 makes the cycle unable to satisfy its own done definition and voids the falsifier. (b) The prediction (mean <= 30.0), the falsifier and the kill threshold all presuppose the loop stopped for a STRUCTURAL reason; there is no rule anywhere for stop_reason = 'deadline', which on this arithmetic is the most likely terminal state, so the most probable outcome is again an unpre-registered post-hoc argument, the exact failure mode cycles 1 and 6 were challenged for and HYP-0011 was supposed to end with its exhaustive verdict table. (c) Worst case is not a null, it is a false ship: contract.json carries ship_at_score = 33.0 and the engine auto-fires on it (JOURNAL 2026-09-22 02:35:14Z, 'ship condition met: incumbent 8.849 <= 11.3 -> consolidate + paper'). A deadline stop after 2-3 accepted removals returns C2 ~= 32 at F >= F35 on all five, mean score ~32, which trips ship_at_score and sends the campaign to consolidate + paper with 32 recorded as 'the cheapest preparation reaching anchor fidelity' when 32 is only an upper bound set by the clock. Required before compute: measure the per-iteration cost in path as EXP-0013 does and SIZE the schedule to the deadline instead of hard-coding 8 x 3000 / 400 / 4 x 1500; guarantee round 1 completes (it is the falsifier's evidence) by budgeting it first and shrinking later rounds, never the reverse; and pre-register the verdict for stop_reason = 'deadline' explicitly, stating that a deadline-truncated cost is an UPPER BOUND on the crossing and does not confirm the prediction, does not fire the kill, and must not be presented as the crossing cost even if the mean lands under 33.

OBJECTION 2 (BLOCKING, inference): the kill threshold retires the direction permanently on evidence that cannot distinguish a structural limit from a one-start local optimum, and the reopen condition explicitly excludes that case.

Section 5.3 gives each tentative removal ONE warm start from the current accepted theta and 400 iterations. Deleting a CNOT is a large discrete perturbation of the circuit, so that warm start is a poor initialisation, and L-BFGS-B will generically terminate at a nearby local optimum with status 0 and a gradient infinity-norm far below 1e-6. status 0 certifies local stationarity, nothing more. But the kill threshold fires precisely on 'all first-round tentative refits recorded as CONVERGED (status 0 or gradient infinity-norm <= 1e-6)' and then declares the block minimal at 35 and instructs that no block-pruning variant be proposed in cycle 15. Reopen condition (A) is gated on status = maxiter or gmax > 1e-6, i.e. it covers only the non-converged case, so a first round that is uniformly converged-but-local kills the direction with no priced reopening path at all. That is the highest-cost failure mode available in this design, and it is more likely than the structural reading.

It also inverts the contract's own fairness anchor. The bar being cleared, F35, was fitted at 6 inits x 3000 L-BFGS-B iterations with a pre-registered 12-init escalation (CONTRACT.md BASELINE FAIRNESS HOLD), and Section 5.2 rightly gives the full structure 8 x 3000 here. Every PRUNED structure is then asked to clear that bar on 1 start x 400 iterations, about 1/60th of the anchor's fit budget. A shortfall under that asymmetry is not evidence about structure. Required before compute: either give round 1 at least 2-3 starts per slot (warm start plus random draws from a pinned seed, with the round-1 budget sized per objection 1), or, if the 1-start round is kept as the cheap screen, forbid the kill from firing on it alone and require a pre-registered multi-restart re-check (e.g. the top-5 slots by best F_trial at >= 8 starts x 3000 iterations) before the kill is accepted; and widen reopen condition (A) to cover converged-to-a-local-optimum, since a multi-restart study lifting a status-0 slot above THRESH is exactly the same under-optimisation finding as a maxiter slot and must not be excluded by termination status. (HYP-0014)
- [ALIVE] ELI5: the grader changed its mind about what counts as winning, and almost nothing in the campaign has caught up to that yet. Until now the goal was 'match the 89-92% reference state as closely as you can', and cycle 13 finally did it: it reproduced the campaign's own sealed anchor fidelity to about two ten-thousandths, which also proved (measured, not argued) that the target construction taken straight from the contract's Metric line is the right one. The new goal is different and harder to fake: reaching the anchor fidelity is now worth zero credit by itself, and the only thing scored is HOW CHEAP the circuit is that reaches it. Cost here means the number of two-qubit entangling gates, the expensive ones on real hardware. The reference circuit spends exactly 35 of them. So the question this cycle asks is the simplest possible version of the campaign's actual scientific question: are all 35 of those gates actually doing work, or is the reference wasteful? The way to find out is to delete them one at a time. Take the reference circuit, try removing each single two-qubit gate, re-tune the 96 rotation knobs around the hole, and keep the deletion that hurts least. If the re-tuned circuit still reaches anchor fidelity, the gate was waste: accept the deletion and try again on what is left. Stop the first time no single remaining gate can be removed without dropping below the anchor. The number of gates left at that moment IS the answer the contract asks for, the cheapest preparation that still reaches anchor fidelity, and the trace of (cost, fidelity) pairs collected on the way down IS the fidelity-vs-cost curve the contract wants recorded. Why this and not the paper's ma-QAOA + SAP/OSAP route first: I priced that route analytically before proposing anything, and it does not fit through the door yet. Each of the 2K = 24 four-body Pauli strings in H_cost costs 6 CNOTs to exponentiate (2(w-1) for weight w = 4), so ONE ma-QAOA layer costs about 144 two-qubit gates plus 1 for the |I> reference. The contract declares anything above 70 INVALID, so an un-pruned p = 1 ma-QAOA candidate cannot even be scored: it needs roughly 80% of its terms pruned away just to become a legal submission, and roughly 96% pruned (about 5 surviving four-body terms) to land under 35. That is a real and interesting measurement, but it is a large search and it must not be the first thing attempted under a metric no candidate has ever been scored against. This cycle buys the cheap, decisive bit first: whether the 35-gate anchor circuit is itself compressible, on the one structure family the campaign has already validated end to end. Formally: branching from EXP-0013, the candidate rebuilds the target unchanged (H_tot dense from payload['h_cost'] with leftmost Pauli label character = qubit 0, T = normalize(expm(-beta*H_tot/4) @ payload['reference_state']) at beta = payload['beta'], which is CONTRACT.md's frozen Metric line read literally and needs no Majorana algebra, no Jordan-Wigner choice and no kernel solve), then runs greedy backward elimination over a boolean mask on the 35 CNOT slots of the hardware-efficient block (6 layers of rz/rx on 8 qubits interleaved with 5 linear chains cx(q,q+1), q = 0..6, after layers 0..4), with the contract-mandated 8-Hadamard prefix that costs 0 on the two-qubit axis, refitting theta of shape (6,8,2) by adjoint-gradient L-BFGS-B at every tentative removal, accepting a removal only when the refit self-fit fidelity clears the CONTRACT-published per-instance anchor F35 plus a 0.002 margin, under a wall-clock deadline guard that always returns a valid circuit. The structural change is deliberate and is the whole hypothesis: the incumbent's byte-identical-topology constraint, correct for confirming the convention, is exactly what the new metric makes worthless, so it is dropped here for the first time. (Δ=-2.6019) — mechanism: The 35-CNOT hardware-efficient block is over-parameterized for these beta=3 sparse-SYK TFD targets: the anchor fidelity F35 is set by the expressivity of the layered rz/rx-plus-linear-chain family, not by the full CNOT c (EXP-0014)
- [OPEN] ELI5: last cycle's circuit-slimming worked, but the score barely moved, and the arithmetic says why. The grader adds two things together: how many expensive two-qubit gates the circuit spends, and a 1000x-weighted penalty for any fidelity shortfall below the published anchor. Last cycle's run removed about 13 gates spread over four of the five instances (worth about 13 points of the 5-instance total), yet the total only fell by 13.01/5 = 2.60 per instance, because roughly 22 points of the total are NOT cost at all: they are a fidelity shortfall of about 0.022 sitting on ONE instance. Reconstructing it from the two scored runs: cycle 13 emitted the full 35-gate circuit on all five instances and scored 39.5234 per instance, so its total shortfall was (39.5234-35)*5/1000 = 0.0226 of fidelity; cycle 14 kept exactly that shortfall (36.9214*5 = 184.61, minus about 127.0 of cost over the four pruned instances, leaves about 57.6 on the fifth, i.e. 35 gates plus 22.6 penalty points) and paid for its whole improvement out of cost alone. The reason that instance is stuck is written into the candidate: lines 479-485 of the incumbent ABORT the whole pruning loop the moment the full-35-gate fit lands more than 0.001 below that instance's anchor, so the one instance carrying the penalty is also the one instance that is never allowed to remove a single gate. It keeps the debt AND all 35 gates: the worst of both. That abort was a sensible safety rule under the OLD metric, where fidelity was the whole score. Under the current metric it is simply an arithmetic mistake, because one gate is worth 1 point and 0.001 of fidelity is worth 1 point, so a gate removal that costs less than 0.001 of fidelity is a net gain EVEN ON an instance that is already in debt. This cycle therefore replaces every threshold, margin and abort in the selection logic with direct minimization of the grader's own published formula, which the candidate is allowed to compute because the five anchor fidelities are printed in CONTRACT.md's Metric paragraph (0.8955, 0.9164, 0.9011, 0.9228, 0.9229) and are public contract data, not battery data. Formally: branching from EXP-0014, the candidate keeps the target construction, the 8-Hadamard prefix, the masked hardware-efficient block, the adjoint-gradient L-BFGS-B fit primitive and the topology gate byte-identical, and changes only the decision rule, from 'accept a removal iff refit F >= anchor + 0.002, abort if the full-mask fit is below anchor - 0.001, emit the last accepted mask' to 'evaluate a point, score it with s_hat = 1000*max(0, anchor + 0.0005 - F_self) + C2, accept the removal that minimizes s_hat whenever it beats the current point's s_hat, never abort on a debt instance, record EVERY evaluated (mask, theta) point in a trace, and emit the arg-min of s_hat over that entire trace'. Emitting the trace arg-min makes the result monotone by construction: the full-mask best-angle point is always in the trace, so this candidate can never emit a point worse than the best point it has seen, which is the structural repair for the two ways cycle 14 could throw work away (a deadline-truncated round breaking out with nothing, and a consolidation regression discarding the round). The freed budget goes where the diagnosis says the points are: any instance whose full-mask fit lands below its anchor gets a pre-registered escalated multistart (the deficit is a local-optimum basin miss, not a structural limit, and the campaign's own sealed anchor for Row 30 needed the Director's 12-init escalation to be found at all, per JOURNAL 2026-09-21 23:20Z), and the escalation is driven by the same s_hat so it is the same decision rule, not a second mechanism. — challenged (advisory: proceeding to coder): Two blocking objections. Neither is about the diagnosis: I checked the arithmetic and it holds. EXP-0014's verdict.json gives incumbent 39.5233897 (cycle-13 circuit re-baselined under the new metric) and candidate 36.9214495, so cycle 13's total was 197.617 = 175 cost + 22.617 penalty points, i.e. 0.022617 of fidelity debt, and cycle 14's 184.607 is consistent with ~127 cost on four instances plus 35+22.6 on a fifth. I verified the abort at workspace/incumbent.py:479-485 fires exactly when f_full < anchor - 0.001 and returns the full 35-cx circuit before the descent ever runs, so the debt-carrying instance is structurally barred from removing a gate, and I verified MARGIN=0.002 (line 81), ANCHOR (lines 84-90), the argmax-fidelity selection (line 521) and the thresh tests at 522/533 are the only selection logic. Replacing all of it with direct s_hat minimization is the right repair, and the 1-point-per-gate vs 1-point-per-0.001-fidelity exchange rate is stated correctly.

OBJECTION 1 (blocking, arithmetic, free to fix): s_hat is DEGENERATE above the anchor, so B5(a) makes the descent fidelity-blind exactly where the incumbent's four pruned instances live. s_hat = 1000*max(0, anchor+SAFETY-F) + C2 returns exactly cost-1 for EVERY trial removal whose refit clears anchor+SAFETY: the first term is exactly 0.0, an exact float tie, not a near-tie. min() over the trial list then breaks that tie by iteration order, i.e. by slot index, so on any instance not in debt the accepted removal is the lowest-indexed feasible slot rather than the least-damaging one. The least-damage ranking (incumbent line 521, max over f_trial) is precisely the mechanism that bought cycle 14's ~13 removals: each round spends its fidelity headroom on the cheapest-damage slot, and that headroom is what funds the NEXT round. Removing an arbitrary feasible slot instead burns headroom faster and stops the descent earlier. Two consequences the brief gets wrong. (a) Prediction (3), 'the four already-pruned instances do not regress, because the trace arg-min contains their cycle-14 endpoints', is false as stated: the trace contains only points THIS run evaluates, and under a different accept rule the descent walks a different path, so cycle 14's endpoints need never appear in it. The arg-min guarantee is monotonicity against this run's own best point, not against the incumbent. (b) A regression on those four instances is therefore a live outcome, and under the pre-registered kill threshold (a) it would read as 'the score decomposition is wrong' when it is in fact a tiebreak defect. Fix, free: make the selection key lexicographic, minimize (s_hat, -f_trial), so s_hat decides whenever it discriminates and the incumbent's least-damage rule decides on the plateau; apply the same secondary key to the best_point update in B3/B5(d) (strictly-lower s_hat, then strictly-higher F_self), and restate prediction (3) as 'do not regress because the same least-damage descent runs with a strictly larger accept set', which is then actually true.

OBJECTION 2 (blocking, free): B6 is not executable as written, and the budget change is unsourced. (i) The gate-construction line in B6 reads 'masked_block_gates(p[\'mask\'].reshape... no, p[\'x\'].reshape(LAYERS, BLOCK_QUBITS, 2), p[\'mask\'])' — a mid-expression self-correction left in the brief. Copied literally it is a SyntaxError at import, which the B8 except-Exception guard cannot catch because the guard is inside prepare(), so the cycle returns nothing at all. This campaign has produced two measured experiments in fifteen cycles and cycle 7 died on an infrastructure error, so an un-executable line in the one section that emits the circuit is not a typo to wave through. Restate it as the single form intended: gates = H_PREFIX + masked_block_gates(p['x'].reshape(LAYERS, BLOCK_QUBITS, 2), p['mask']). (ii) SAFETY = 0.0005 is justified in the brief as absorbing a self-fit-vs-scorer disagreement 'EXP-0013 measured ~2e-4', and that citation is wrong: AUDIT.md line 18 records that the 2e-4 is the reproduction of the MEAN of the five sealed anchors and states explicitly that 'the per-instance comparison unrecorded' and 'this cycle plausibly misses it on at least one instance'. No per-instance disagreement has ever been measured. On top of that the ANCHOR values are 4-decimal transcriptions of the sealed F35, so each carries up to 5e-5 of unknown-sign rounding. Cutting the buffer from the incumbent's 0.002 to 0.0005 deliberately parks every above-anchor emission within ~2.5e-4 of a cliff whose height is 1000 points per unit, to buy at most ~1 gate per instance. Either keep SAFETY at 0.002 for this cycle and take the points from the freed abort path (which is where the diagnosis says ~22 of them are), or pre-register SAFETY = 0.0005 together with a DIAG-recorded per-instance test of the disagreement so the next cycle can set it on measurement rather than on a misread audit line. (iii) DEADLINE_S 240 -> 420 makes ~35 minutes of scorer wall clock across five instances with no cited evidence of the scoring harness's per-candidate time limit; I cannot verify one from this worktree. State the limit and its source in B7, or keep 240 s, where B4's escalation gate T_FULL_BUDGET already fits (the full-mask 8x3000 fit is the dominant cost and one escalation stage overruns the 0.35 gate anyway, so ESC_SEEDS=(11,12,13) is in practice one stage, which the brief should say outright rather than implying three). (HYP-0015)
- [OPEN] ELI5: cycle 15 was not wrong, it was never run. HYP-0015 was pre-registered, challenged (advisory PROCEED) and then no candidate file and no score were ever produced, exactly the way cycle 12 vanished before cycle 13 re-ran it. So this cycle re-registers that design in full, standalone, and adds the one thing that changed underneath it: at 04:09Z the Director re-pinned the contract to run the five instances in parallel and to REQUIRE that a candidate use a fixed iteration budget and seed every RNG on payload['name'], never on wall-clock time or call order. HYP-0015's budget was a 420-second wall-clock deadline, which that rule now forbids, so the deadline is replaced by a deterministic fit-count budget and every seed is keyed to the instance name. Everything else is the cycle-15 diagnosis, restated. The grader adds two things: how many expensive two-qubit gates the circuit spends, plus a 1000x-weighted penalty for any fidelity shortfall below the published anchor for that instance. Cycle 14 removed about 13 gates spread over four of the five instances yet the mean only fell 2.60, because roughly 22 of the remaining points are not cost at all: they are about 0.022 of fidelity debt sitting on ONE instance. The arithmetic checks out against the two scored runs: cycle 13 emitted the full 35-gate circuit everywhere and scored 39.5234 per instance, so its total debt was (39.5234-35)*5/1000 = 0.0226 of fidelity; cycle 14's 184.61 total is 35 gates plus about 22.6 penalty points on one instance plus about 127 gates over the other four. The reason that instance is stuck is a line of code: workspace/incumbent.py:479-485 ABORTS the whole pruning loop whenever the full-35-gate fit lands more than 0.001 below that instance's anchor, so the one instance carrying the penalty is the one instance never allowed to delete a single gate. It keeps the debt AND all 35 gates, the worst of both. That abort was correct under the OLD fidelity-only metric. Under the current metric it is an arithmetic mistake: one gate is worth 1 point and 0.001 of fidelity is worth 1 point, so a removal that costs less than 0.001 of fidelity is a net gain EVEN on an instance already in debt. This cycle therefore deletes every threshold, margin and abort in the selection logic and replaces them with direct minimization of the grader's own published formula, which the candidate may compute because the five anchor fidelities are printed in CONTRACT.md's public Metric paragraph (0.8955, 0.9164, 0.9011, 0.9228, 0.9229) and are contract data, not battery data. Formally: keep the EXP-0014 target construction (H_tot dense from payload['h_cost'] with leftmost Pauli label character = qubit 0, T = normalize(expm(-beta*H_tot/4) @ payload['reference_state'])), the 8-Hadamard prefix that costs 0 on the two-qubit axis, the masked hardware-efficient block (6 layers of rz/rx on 8 qubits interleaved with 5 linear chains cx(q,q+1), q=0..6, slot s = 7*layer+q), the adjoint-gradient L-BFGS-B fit primitive, the import-time topology and gradient gates, all byte-identical; change only (a) the decision rule, to s_hat = 1000*max(0, anchor + 0.0005 - F_self) + C2 driving accept, reject and emit, with every evaluated (mask, theta) point recorded in a trace and the ARG-MIN of that trace emitted, so the run can never emit a point worse than the best point it has seen; (b) the abort, deleted, replaced by a pre-registered escalated multistart on any instance whose full-mask fit lands below its anchor (the sealed anchor for Row 30 itself needed the Director's 12-init escalation to be found, JOURNAL 2026-09-21 23:20Z, so a deficit is a basin miss, not evidence of a structural limit); and (c) the budget, from wall-clock seconds to a fixed count of trial fits with name-keyed seeds, as the re-pinned contract now requires. — challenged (advisory: proceeding to coder): Two blocking objections. Neither is about the diagnosis, which I re-checked and which holds: EXP-0014/verdict.json gives incumbent 39.5233897 and candidate 36.9214495, and I verified at workspace/incumbent.py:478-485 that the abort fires exactly when f_full < anchor - 0.001 and returns the full 35-cx circuit before the descent runs, so the debt-carrying instance is structurally barred from removing a gate. Deleting that abort and replacing MARGIN/thresh (incumbent.py:81, 460-461, 522, 533) with direct s_hat minimization is the right repair, the 1-gate-per-0.001-fidelity exchange rate is stated correctly, the ANCHOR values (incumbent.py:84-90) match CONTRACT.md's public Metric paragraph exactly, fit()'s signature (theta0, mask, maxiter, n_restarts, restart_seed, target) matches every call site in B2/B5/B6, sum(16..35) = 510 <= MAX_TRIAL_FITS = 520 with MAX_ROUNDS = 20 is arithmetically consistent, FULL_INITS 8 + ESC_STAGES 3 x ESC_INITS 8 = 32 matches the reopen_condition's 32-init budget, and this is not a duplicate: workspace/ contains only baseline.py and incumbent.py, so HYP-0015 genuinely produced no candidate file.

OBJECTION 1 (BLOCKING): the s_hat arg-min selection in B6(b) is DEGENERATE on the plateau, and the plateau is exactly where cycle 14's accepted removals lived. Within one round every trial shares the same cost - 1, so s_hat(f_trial, cost-1, anchor) = 1000*max(0, anchor + 0.0005 - f_trial) + (cost - 1). Once a trial refit clears anchor + SAFETY the penalty term is identically zero, so EVERY such slot has the SAME s_hat and the arg-min is decided by iteration order (lowest live slot index), not by fidelity. This is not hypothetical: cycle 14 accepted a removal only when the refit cleared anchor + MARGIN = anchor + 0.002, which is strictly above anchor + SAFETY = anchor + 0.0005, and it accepted about 3 such removals on each of 4 instances. Every one of those accepted rounds was therefore inside the flat region, and in any round where two or more slots clear the anchor the new rule discards the fidelity headroom that later rounds consume. Greedy backward elimination's only engine is 'remove the gate that hurts least'; B6(b) removes that engine precisely where the incumbent was winning, which directly threatens prediction (3) ('the four already-pruned instances do not regress'). B3's instruction that 'no other comparison of a fidelity to a threshold may exist in prepare()' forbids the obvious workaround, so the fix must be inside the objective: select lexicographically on (s_hat, -f_trial), i.e. minimize s_hat and break ties by maximizing f_trial. That reduces exactly to the incumbent's argmax-fidelity rule on the plateau and to pure s_hat minimization in the debt region, so it is strictly the union of the two behaviours the cycle wants, and it costs one sort key. Apply the same tiebreak to the trace arg-min in B4/B7 (best_point updated on strictly-less s_hat, tie broken by higher F_self, then by earlier tag) so the emitted point is not order-dependent either.

OBJECTION 2 (BLOCKING): the budget is re-denominated from seconds to fit counts without a single measured second, and the run now has to fit a hard 3600 s eval timeout that no prior cycle has had to respect. contract.json:43 declares eval_timeout_s = 3600 (mirrored at FACTS.json:151), and since commit 99151092 the scorer runs the five instances as five concurrent forked processes on one Mac mini, so every instance's compute now competes for the same box under one shared 60-minute cap. HYP-0016's per-instance budget is 520 trial fits at 400 iterations, plus up to 20 consolidation fits at 4 inits x 1500 iterations, plus a full fit at 8 x 3000, plus up to 24 escalation fits at 3000 iterations. The only calibration anywhere in the campaign is cycle 14's 240 s wall-clock cap, under which each instance completed roughly 3 rounds (about 102 trial fits) before stopping, implying on the order of 2 s per 400-iteration trial fit and therefore roughly 1000 to 1300 s of trial fits alone, before consolidation and the 24 escalation fits at 7.5x the iteration count. That is a 5x to 10x increase in per-instance compute, now multiplied by five-way contention, against a cap that kills the cycle outright. A timeout yields NO score, which is this campaign's dominant failure mode: 12 of 16 cycles have produced no measurement. The contract's own budget clause and the search playbook require the budget stated in units that are measured and shown to fit the clock; 'MAX_TRIAL_FITS = 520' with the comment '510 trial fits fit inside this' states only that the counter does not bind, never that the wall clock does not. Fix, cheap and free of any wall-clock dependence inside the candidate: (a) measure seconds per fit offline before the run by calling the incumbent's own fit() on a random normalized 256-dim target at 400x1, 1500x4 and 3000x8, and record the three numbers in the hypothesis; (b) set MAX_ROUNDS and MAX_TRIAL_FITS from that measurement so that the worst-case per-instance total, times a contention factor of at least 2 for five concurrent processes, lands under 1800 s, i.e. at least 2x headroom under the 3600 s cap (on the 2 s-per-trial-fit estimate that is roughly 10 rounds, 305 trial fits, not 20 rounds and 520); (c) keep diag['wall_s'] as a pure measurement so the next cycle can raise the count on evidence. The fixed-count, name-keyed-seed design is correct and is what the re-pinned contract requires; only the VALUE of the count is unjustified.

NON-BLOCKING NOTE, fix while you are in there: B2's name_seed packs the offset into a 64-wide block (h*64 + offset), but the consolidation call site uses offset 102 + cost_after, which runs 117 to 136 and overflows that block, aliasing instance n's consolidation seeds onto the reserved range of any name whose hash is h+2 or h+3. With five names the collision is almost certainly empty and nothing about determinism breaks, but the invariant the formula advertises is false as written. Either widen the multiplier to 1024 or hash the (name, offset) pair together. (HYP-0016)
- [ALIVE] ELI5: cycle 15 was not wrong, it was never run. HYP-0015 was pre-registered, challenged (advisory PROCEED) and then no candidate file and no score were ever produced, exactly the way cycle 12 vanished before cycle 13 re-ran it. So this cycle re-registers that design in full, standalone, and adds the one thing that changed underneath it: at 04:09Z the Director re-pinned the contract to run the five instances in parallel and to REQUIRE that a candidate use a fixed iteration budget and seed every RNG on payload['name'], never on wall-clock time or call order. HYP-0015's budget was a 420-second wall-clock deadline, which that rule now forbids, so the deadline is replaced by a deterministic fit-count budget and every seed is keyed to the instance name. Everything else is the cycle-15 diagnosis, restated. The grader adds two things: how many expensive two-qubit gates the circuit spends, plus a 1000x-weighted penalty for any fidelity shortfall below the published anchor for that instance. Cycle 14 removed about 13 gates spread over four of the five instances yet the mean only fell 2.60, because roughly 22 of the remaining points are not cost at all: they are about 0.022 of fidelity debt sitting on ONE instance. The arithmetic checks out against the two scored runs: cycle 13 emitted the full 35-gate circuit everywhere and scored 39.5234 per instance, so its total debt was (39.5234-35)*5/1000 = 0.0226 of fidelity; cycle 14's 184.61 total is 35 gates plus about 22.6 penalty points on one instance plus about 127 gates over the other four. The reason that instance is stuck is a line of code: workspace/incumbent.py:479-485 ABORTS the whole pruning loop whenever the full-35-gate fit lands more than 0.001 below that instance's anchor, so the one instance carrying the penalty is the one instance never allowed to delete a single gate. It keeps the debt AND all 35 gates, the worst of both. That abort was correct under the OLD fidelity-only metric. Under the current metric it is an arithmetic mistake: one gate is worth 1 point and 0.001 of fidelity is worth 1 point, so a removal that costs less than 0.001 of fidelity is a net gain EVEN on an instance already in debt. This cycle therefore deletes every threshold, margin and abort in the selection logic and replaces them with direct minimization of the grader's own published formula, which the candidate may compute because the five anchor fidelities are printed in CONTRACT.md's public Metric paragraph (0.8955, 0.9164, 0.9011, 0.9228, 0.9229) and are contract data, not battery data. Formally: keep the EXP-0014 target construction (H_tot dense from payload['h_cost'] with leftmost Pauli label character = qubit 0, T = normalize(expm(-beta*H_tot/4) @ payload['reference_state'])), the 8-Hadamard prefix that costs 0 on the two-qubit axis, the masked hardware-efficient block (6 layers of rz/rx on 8 qubits interleaved with 5 linear chains cx(q,q+1), q=0..6, slot s = 7*layer+q), the adjoint-gradient L-BFGS-B fit primitive, the import-time topology and gradient gates, all byte-identical; change only (a) the decision rule, to s_hat = 1000*max(0, anchor + 0.0005 - F_self) + C2 driving accept, reject and emit, with every evaluated (mask, theta) point recorded in a trace and the ARG-MIN of that trace emitted, so the run can never emit a point worse than the best point it has seen; (b) the abort, deleted, replaced by a pre-registered escalated multistart on any instance whose full-mask fit lands below its anchor (the sealed anchor for Row 30 itself needed the Director's 12-init escalation to be found, JOURNAL 2026-09-21 23:20Z, so a deficit is a basin miss, not evidence of a structural limit); and (c) the budget, from wall-clock seconds to a fixed count of trial fits with name-keyed seeds, as the re-pinned contract now requires. (Δ=-11.7214) — mechanism: The residual score was never a physics limit: it was a control-flow guard whose semantics were inherited from the old fidelity-only metric. Under the current score s = 1000*max(0, anchor - F) + C2, one gate and 0.001 of  (EXP-0016)

